# 02 Hormuz Update2

<div style="font-family:'Inter',-apple-system,BlinkMacSystemFont,sans-serif;background:#0d1117;border-radius:12px;padding:0;line-height:1.4;box-shadow:0 4px 24px rgba(0,0,0,0.5);">
<div style="border-bottom:1px solid #21262d;padding:12px 24px;display:flex;justify-content:space-between;align-items:center;background:#161b22;border-radius:12px 12px 0 0;">
<span style="font-size:9pt;font-weight:700;letter-spacing:1.2px;text-transform:uppercase;color:#d62728;">Quantitative Event Study // Update 2 · June 14, 2026</span>
<div style="display:flex;gap:8px;align-items:center;">
<span style="font-size:7.5pt;color:#6e7681;background:#0d1117;padding:2px 8px;border-radius:20px;border:1px solid #21262d;">Pre-ceasefire snapshot</span>
<span style="font-size:8pt;font-family:'SF Mono',monospace;color:#58a6ff;background:#0d1117;padding:3px 10px;border-radius:20px;">NB-2026-SHOCK-U2</span>
</div>
</div>
<div style="padding:28px 32px 24px;">
<div style="margin-bottom:6px;font-size:8pt;font-weight:800;letter-spacing:1.5px;text-transform:uppercase;color:#d62728;">HORMUZ 2026 — UPDATE 2</div>
<h1 style="font-size:26pt;font-weight:800;margin:0 0 4px 0;letter-spacing:-0.5px;color:#f0f6fc;">When Diplomacy Decoupled From Supply</h1>
<p style="font-size:11pt;color:#8b949e;margin:0 0 20px 0;padding-left:14px;border-left:3px solid #d62728;">5-Phase Analysis · 16 Assets · Data Cutoff Jun 13, 2026 · Pre-Ceasefire Snapshot</p>
<div style="display:grid;grid-template-columns:repeat(4,1fr);gap:10px;margin-bottom:20px;">
<div style="background:#161b22;border:1px solid #30363d;border-top:2px solid #6e7681;padding:12px;border-radius:6px;"><div style="font-size:8pt;text-transform:uppercase;color:#6e7681;margin-bottom:4px;letter-spacing:0.6px;">Study Window</div><div style="font-size:13pt;font-weight:700;color:#f0f6fc;">Jan–Jun 2026</div><div style="font-size:7.5pt;color:#484f58;margin-top:2px;">5 phases · ~113 trading days</div></div>
<div style="background:#161b22;border:1px solid #30363d;border-top:2px solid #58a6ff;padding:12px;border-radius:6px;"><div style="font-size:8pt;text-transform:uppercase;color:#6e7681;margin-bottom:4px;letter-spacing:0.6px;">Universe</div><div style="font-size:13pt;font-weight:700;color:#f0f6fc;">16 Assets</div><div style="font-size:7.5pt;color:#484f58;margin-top:2px;">10 original + 6 new</div></div>
<div style="background:#1a0a00;border:1px solid #ff7f0e44;border-top:2px solid #ff7f0e;padding:12px;border-radius:6px;"><div style="font-size:8pt;text-transform:uppercase;color:#ff7f0e;margin-bottom:4px;letter-spacing:0.6px;">Central Finding</div><div style="font-size:13pt;font-weight:700;color:#ff7f0e;">May Paradox</div><div style="font-size:7.5pt;color:#8b6000;margin-top:2px;">WTI −20% · strait still closed</div></div>
<div style="background:#0a1a0a;border:1px solid #2ca02c44;border-top:2px solid #2ca02c;padding:12px;border-radius:6px;"><div style="font-size:8pt;text-transform:uppercase;color:#2ca02c;margin-bottom:4px;letter-spacing:0.6px;">Cutoff</div><div style="font-size:13pt;font-weight:700;color:#2ca02c;">Jun 13, 2026</div><div style="font-size:7.5pt;color:#006b00;margin-top:2px;">Ceasefire Jun 21–25 (after cutoff)</div></div>
</div>
<div style="display:flex;flex-wrap:wrap;gap:6px;">
<span style="background:#1c1116;border:1px solid #d6272844;padding:4px 10px;border-radius:20px;font-size:8pt;color:#d62728;">WTI: primary vector</span>
<span style="background:#1c1116;border:1px solid #ff7f0e44;padding:4px 10px;border-radius:20px;font-size:8pt;color:#ff7f0e;">May Paradox: diplomacy beat supply</span>
<span style="background:#1c1116;border:1px solid #39397944;padding:4px 10px;border-radius:20px;font-size:8pt;color:#818cf8;">ITA: defense backlog trade</span>
<span style="background:#1c1116;border:1px solid #006d7744;padding:4px 10px;border-radius:20px;font-size:8pt;color:#2dd4bf;">BWET: third decoupling</span>
<span style="background:#1c1116;border:1px solid #5aabff44;padding:4px 10px;border-radius:20px;font-size:8pt;color:#5aabff;">TLT: bond safe-haven test</span>
<span style="background:#1c1116;border:1px solid #b8860b44;padding:4px 10px;border-radius:20px;font-size:8pt;color:#b8860b;">Aramco: sovereign exporter</span>
<span style="background:#1c1116;border:1px solid #6e767144;padding:4px 10px;border-radius:20px;font-size:8pt;color:#6e7681;">Jun 10 re-escalation: second shock test</span>
</div>
</div>
<div style="background:#161b22;border-top:1px solid #21262d;padding:10px 24px;display:flex;justify-content:space-between;align-items:center;border-radius:0 0 12px 12px;">
<div style="display:flex;gap:20px;font-size:8.5pt;color:#6e7681;"><span><span style="color:#8b949e;">Developer</span> Abdallah A Khames</span><span><span style="color:#8b949e;">Company</span> BODZZ</span><span style="font-family:monospace;"><span style="color:#8b949e;">GitHub</span> abdallah-bodzz</span></div>
<div style="font-size:8pt;font-family:monospace;color:#58a6ff;background:#0d1117;padding:3px 10px;border-radius:20px;border:1px solid #21262d;">v1 · jun 2026 · update 2</div>
</div>
</div>

## Part 0: Executive Summary

### The Story So Far

Update 1 closed on April 30 with a clean thesis: physical oil decoupled from financial assets during the 33-day Hormuz closure. WTI rose +32.9% while energy equities, gold, and airlines diverged or fell. The mechanism was physical scarcity — the strait carried only ~2% of normal transit volume.

### What Update 2 Adds

**The May Paradox.** WTI fell nearly 20% in May — its worst month since COVID. The strait remained at ~2% capacity throughout. Physical supply conditions were unchanged. What changed was the forward expectation of peace. Ceasefire diplomacy overrode scarcity pricing. Markets started pricing the barrel that *would* move through an eventually re-opened strait rather than the barrel that *couldn't* move today.

**The June 10 Re-Escalation.** Iran announced a full commercial closure on June 10. WTI spiked on Trump's ceasefire rejection, then fell on renewed talks. This is the second testable shock event — same framework, measurable whether the market had learned a playbook.

**Three new analytical stories run in parallel:**

| # | Finding | Evidence |
|---|---------|---------|
| 1 | **May Paradox** — diplomacy decoupled from supply | WTI −20% in May, strait still closed |
| 2 | **Defense is a backlog trade** — not a war trade | ITA held premium through correction |
| 3 | **Third decoupling** — freight vs oil | BWET >> WTI cumulative return |

---

### 5-Window Architecture

| Phase | Dates | Trading Days | Primary Driver |
|-------|-------|-------------|----------------|
| Pre-event | Jan 1–Feb 27 | 39 | Escalation pricing building |
| Shock/Closure | Feb 28–Apr 16 | 33 | Physical scarcity dominant |
| False Dawn | Apr 17–Apr 30 | 9 | Peace signal, swift repricing |
| **Paradox Correction** | May 1–May 29 | ~21 | **Diplomacy beats supply** |
| Volatile Diplomacy | Jun 1–Jun 13 | ~9 | Re-escalation + ceasefire talks |

---

> **Static snapshot — June 13, 2026.** Iran–Israel ceasefire announced June 21–25 (after this cutoff). Update 3 covers the resolution chapter.

## Part 1: Setup & Data — 16 Assets, 5 Windows

**Cell 1:**
```python
# ── Execution config ─────────────────────────────────────────────────────────
CACHE_DATA   = True    # True = load _u2 parquet. False = re-fetch from yfinance.
SAVE_FIGURES = True    # True = save PNGs to outputs/update_2/charts/
FIGURE_DPI   = 150

# ── Imports ──────────────────────────────────────────────────────────────────
import sys, os, warnings
warnings.filterwarnings('ignore')

import numpy as np
import pandas as pd
import matplotlib
import matplotlib.pyplot as plt
import matplotlib.patches as mpatches
import matplotlib.ticker as mtick
import matplotlib.dates as mdates
import matplotlib.gridspec as gridspec
import matplotlib.patheffects as pe
from matplotlib.lines import Line2D
from matplotlib.colors import LinearSegmentedColormap
import matplotlib.colors as mcolors
import seaborn as sns
from IPython.display import display, HTML

# ── Dark chart theme — matches dashboard ─────────────────────────────────────
plt.style.use('dark_background')
matplotlib.rcParams.update({
    'figure.dpi':        100,
    'savefig.dpi':       FIGURE_DPI,
    'font.family':       'sans-serif',
    'axes.spines.top':   False,
    'axes.spines.right': False,
    'axes.facecolor':    '#0d1117',
    'figure.facecolor':  '#0d1117',
    'text.color':        '#c9d1d9',
    'axes.labelcolor':   '#8b949e',
    'xtick.color':       '#6e7681',
    'ytick.color':       '#6e7681',
    'axes.edgecolor':    '#21262d',
    'grid.color':        '#21262d',
    'grid.alpha':         0.4,
    'legend.frameon':    False,
})
sns.set_context('notebook', font_scale=1.0)

# ── Paths ─────────────────────────────────────────────────────────────────────
PROJECT_ROOT = os.path.abspath(os.path.join(os.getcwd(), '..'))
SRC_DIR      = os.path.join(PROJECT_ROOT, 'src')
DATA_DIR     = os.path.join(PROJECT_ROOT, 'data')
OUT_DIR      = os.path.join(PROJECT_ROOT, 'outputs', 'update_2', 'charts')
CSV_DIR      = os.path.join(PROJECT_ROOT, 'outputs', 'update_2', 'data')
RAW_U2       = os.path.join(DATA_DIR, 'raw', 'prices_2016_2026_u2.parquet')

if SRC_DIR not in sys.path:
    sys.path.insert(0, SRC_DIR)
for d in [OUT_DIR, CSV_DIR]:
    os.makedirs(d, exist_ok=True)

print(f'Project root : {PROJECT_ROOT}')
print(f'Chart output : {OUT_DIR}')
print(f'U2 parquet   : {RAW_U2} | exists={os.path.exists(RAW_U2)}')
```

**Output:**
```
Project root : c:\Users\User\Desktop\2026-hormuz-blockade-analysis
Chart output : c:\Users\User\Desktop\2026-hormuz-blockade-analysis\outputs\update_2\charts
U2 parquet   : c:\Users\User\Desktop\2026-hormuz-blockade-analysis\data\raw\prices_2016_2026_u2.parquet | exists=True
```

**Cell 2:**
```python
import data_fetcher
import event_study
from utils import (
    simple_returns, rolling_volatility, rolling_correlation,
    cumulative_return, oil_gold_ratio, vol_regime_ratio,
    fmt_pct, fmt_x, fmt_window_label, safe_get,
    EVENT_DATES, EVENT_WINDOWS, EVENT_CONFIG,
    CORE_ASSETS, ALL_ASSETS, NEW_ASSETS_U2,
    DEFENSE_ASSETS, FREIGHT_ASSETS, BOND_ASSETS,
    ENERGY_SECTOR, GULF_ASSETS, INTL_ASSETS, DXY_ASSET,
    COLORS, LABELS, WINDOW_COLORS, WINDOW_ALPHA,
    LIMITED_HISTORY_ASSETS, FETCH_END,
    get_band_style, get_baseline_years, limited_history_note,
)

EW          = EVENT_WINDOWS
ED          = EVENT_CONFIG['dates']
WL          = EVENT_CONFIG['window_labels']
PHASE_ORDER = ['pre_event', 'shock', 'reopen', 'correction', 'diplomacy']

# ── Chart palette for phase bands ─────────────────────────────────────────────
PHASE_CFG = {
    'pre_event':  ('#b0b0b0', 0.055, 'Pre-event'),
    'shock':      ('#d62728', 0.090, 'Shock / Closure'),
    'reopen':     ('#2ca02c', 0.090, 'False Dawn'),
    'correction': ('#ff7f0e', 0.085, 'Paradox Correction'),
    'diplomacy':  ('#4a72b0', 0.085, 'Volatile Diplomacy'),
}

# ── Custom diverging colormap for heatmaps ─────────────────────────────────────
HORMUZ_CMAP = LinearSegmentedColormap.from_list(
    'hormuz_div',
    ['#1a3a6b', '#0d1f3c', '#0d1117', '#4a1010', '#d62728'],
    N=512
)

# ── Shared annotation helper ───────────────────────────────────────────────────
def ann(ax, text, xy, xytext, color, fontsize=7.8, fontweight='normal', alpha=0.95):
    ax.annotate(
        text, xy=xy, xytext=xytext,
        textcoords='offset points',
        fontsize=fontsize, fontweight=fontweight, color=color, clip_on=True,
        arrowprops=dict(arrowstyle='->', color=color, lw=1.2, alpha=0.80,
                        shrinkA=0, shrinkB=4),
        bbox=dict(boxstyle='round,pad=0.38', fc='#0d1117',
                  ec=color, alpha=alpha, lw=1.0),
    )

# ── Phase bands on a matplotlib axis ──────────────────────────────────────────
def add_bands(ax, label_top=True, fontsize=7.2):
    ylim = ax.get_ylim(); yr = ylim[1] - ylim[0]
    for wname, (ws, we) in EW.items():
        color, alpha, label = PHASE_CFG.get(wname, ('#888', 0.06, wname))
        ax.axvspan(pd.Timestamp(ws), pd.Timestamp(we),
                   color=color, alpha=alpha, zorder=0, lw=0)
        if label_top:
            mid = pd.Timestamp(ws) + (pd.Timestamp(we) - pd.Timestamp(ws)) / 2
            ax.text(mid, ylim[1] - yr * 0.022, label,
                    ha='center', va='top', fontsize=fontsize,
                    color=color, fontweight='600', style='italic',
                    clip_on=True, zorder=8)
    for ev_label, date in ED.items():
        ax.axvline(pd.Timestamp(date), color='#333',
                   lw=0.7, linestyle=(0, (4, 6)), alpha=0.60, zorder=1)

def style_dates(ax, fmt='%b %y'):
    ax.xaxis.set_major_formatter(mdates.DateFormatter(fmt))
    ax.xaxis.set_major_locator(mdates.MonthLocator())
    plt.setp(ax.xaxis.get_majorticklabels(), rotation=30, ha='right', fontsize=8)

def save_fig(fig, name):
    if SAVE_FIGURES:
        p = os.path.join(OUT_DIR, f'{name}.png')
        fig.savefig(p, dpi=FIGURE_DPI, bbox_inches='tight',
                    facecolor=fig.get_facecolor())
        print(f'  → {p}')

def note(text, color='#8b949e'):
    display(HTML(
        f"<p style='color:{color};font-size:8.5pt;margin:3px 0 8px 0'>"
        f"<i>ℹ {text}</i></p>"))

def callout(text, title='', color='#58a6ff'):
    display(HTML(
        f"<div style='background:#0d1117;border-left:4px solid {color};"
        f"border:1px solid {color}33;padding:12px 16px;border-radius:4px;"
        f"margin:12px 0'>"
        f"{'<b style="color:'+color+'">'+title+'</b><br>' if title else ''}"
        f"<span style='font-size:10pt;color:#c9d1d9'>{text}</span></div>"))

def section_kpi(items):
    """Render a horizontal KPI strip. items = list of (label, value, color)."""
    cells = ''.join(
        f"<div style='flex:1;background:#161b22;border:1px solid #30363d;"
        f"border-top:2px solid {c};padding:10px 14px;border-radius:6px;'>"
        f"<div style='font-size:7.5pt;color:#6e7681;text-transform:uppercase;"
        f"letter-spacing:0.5px;margin-bottom:3px'>{l}</div>"
        f"<div style='font-size:14pt;font-weight:700;color:{c}'>{v}</div></div>"
        for l, v, c in items
    )
    display(HTML(
        f"<div style='display:flex;gap:8px;margin:10px 0 14px 0'>{cells}</div>"))

print('All helpers loaded.')
```

**Output:**
```
All helpers loaded.
```

**Cell 3:**
```python
# ── Load data ─────────────────────────────────────────────────────────────────
if CACHE_DATA and os.path.exists(RAW_U2):
    prices = data_fetcher.load_parquet(RAW_U2)
    print(f'Loaded from cache: {RAW_U2}')
else:
    prices = data_fetcher.run(data_dir=DATA_DIR, force_refresh=True, version='u2')

prices.index = pd.to_datetime(prices.index)
all_av = [a for a in ALL_ASSETS if a in prices.columns]

print(f'Shape      : {prices.shape[0]:,} trading days × {prices.shape[1]} assets')
print(f'Date range : {prices.index[0].date()} → {prices.index[-1].date()}')
print(f'Loaded     : {list(prices.columns)}')
print(f'New assets : {[a for a in NEW_ASSETS_U2 if a in prices.columns]}')
missing = prices.isna().mean().mul(100).round(1)
if missing.any():
    print(f'Missing %  : {missing[missing>0].to_dict()}')
```

**Output:**
```
Loaded from cache: c:\Users\User\Desktop\2026-hormuz-blockade-analysis\data\raw\prices_2016_2026_u2.parquet
Shape      : 3,045 trading days × 16 assets
Date range : 2016-01-04 → 2026-06-12
Loaded     : ['SP500', 'WTI', 'GOLD', 'XOM', 'CVX', 'VIX', 'JETS', 'DXY', 'DAX', 'NKY', 'ITA', 'BWET', 'TLT', 'XLE', 'ARAMCO', 'UNG']
New assets : ['ITA', 'BWET', 'TLT', 'XLE', 'ARAMCO', 'UNG']
Missing %  : {'SP500': 13.8, 'WTI': 13.8, 'GOLD': 13.8, 'XOM': 13.8, 'CVX': 13.8, 'VIX': 13.7, 'JETS': 13.8, 'DXY': 13.7, 'DAX': 13.0, 'NKY': 16.2, 'ITA': 13.8, 'BWET': 74.4, 'TLT': 13.8, 'XLE': 13.8, 'ARAMCO': 46.7, 'UNG': 13.8}
```

**Cell 4:**
```python
# ── Quality report ────────────────────────────────────────────────────────────
report = data_fetcher.quality_report(prices, update_version='2')
callout(
    f'<b>{report["n_assets"]} assets</b> · '
    f'<b>{prices.shape[0]:,} trading days</b> · '
    f'{report["date_range"][0]} → {report["date_range"][1]}<br>'
    f'{report["snapshot_note"]}',
    title='Update 2 · Data Quality',
    color='#ff7f0e'
)
print('Caveats:')
for c in report['caveats']:
    print(f'  · {c}')
```

**Output:**
```
<IPython.core.display.HTML object>
```
```
Caveats:
  · Prices are adjusted for dividends and splits (auto_adjust=True).
  · Survivorship bias: XOM/CVX baselines exclude delisted energy peers.
  · Timezone mismatch: WTI/DXY daily close leads S&P/equity opens -- phantom same-day correlations may exist. CCF analysis partially addresses this.
  · DXY uses ICE futures (DX-Y.NYB). Minor gaps vs spot DXY. Directionally correct.
  · Beta is estimated on ~39 pre-event trading days -- short window. During a 30%+ oil shock, beta is likely non-linear; abnormal returns should be read as directional, not precise.
  · All figures are nominal. No inflation adjustment applied to multi-year price comparisons.
  · Shanghai index excluded: yfinance timezone/holiday handling unreliable for same-day alignment with US markets.
  · BWET (Breakwave Tanker Shipping ETF): launched May 3, 2023. Seasonal baseline covers 2024–2025 only (2 years vs 5 for original assets). Baseline statistics for BWET are marked with a limited-history flag. AUM is ~$25M; low liquidity makes BWET an analytical signal, not a real-world trading recommendation at scale.
  · ARAMCO (2222.SR / Saudi Aramco): trades on Tadawul (Sun–Thu, UTC+3). Reindexed to US trading calendar via forward-fill — same approach as VIX gaps. Same-day comparison with US assets has a structural lag of up to ~18 hours. Treat as directional / window-level indicator, not precise same-day data. Aramco stock reflects Saudi state policy (production decisions, dividends) as much as market fundamentals — frame analysis as 'exporter sovereign response'.
  · UNG (United States Natural Gas Fund): holds near-term nat gas futures. Returns reflect futures performance, subject to contango roll decay. Not equivalent to spot natural gas prices. Long-term UNG holders face structural headwinds from negative roll yield in contango markets.
  · BWET: data available from 2023-05-03 (expected launch: 2023-05-03).
```

## Part 2: What Changed Since April 30 — The Full Timeline

Update 1 ended with WTI at its peak and a clean supply-shock regime. Three things happened afterward that fundamentally change the analytical picture:

**1. The May Paradox (May 1–29):** Strait at 2% capacity. WTI fell ~20% anyway. Diplomacy pricing overrode physical scarcity — the single most surprising finding of the entire 2026 study period.

**2. June 10 Re-escalation:** Iran's second closure announcement. WTI spiked, then fell on renewed ceasefire optimism. This is now the *sixth testable event* the framework can analyze.

**3. New asset signals:** ITA revealed the backlog dynamic. BWET showed a freight premium entirely above and beyond oil prices. TLT tested whether bonds complete the safe-haven failure trifecta.

**Cell 5:**
```python
# ── Five-window return overview — key assets ──────────────────────────────────
ev_stats = event_study.event_window_stats(prices, all_av)

focus_assets = ['WTI','SP500','GOLD','XOM','ITA','BWET','TLT','JETS']
focus_assets = [a for a in focus_assets if a in prices.columns]

piv = (ev_stats[ev_stats['asset'].isin(focus_assets)]
       .pivot(index='asset', columns='window', values='cum_return'))
piv = piv[[c for c in PHASE_ORDER if c in piv.columns]]
piv.index = [LABELS.get(i,i) for i in piv.index]
piv.columns = [WL.get(c,c) for c in piv.columns]

print('=== 5-Window Cumulative Returns (%) ===\n')
display(piv.applymap(lambda x: fmt_pct(x) if pd.notna(x) else '—'))

# KPI strip for the May Paradox
if 'WTI' in prices.columns:
    c_wti = prices.loc['2026-05-01':'2026-05-29','WTI'].dropna()
    c_sp  = prices.loc['2026-05-01':'2026-05-29','SP500'].dropna() if 'SP500' in prices.columns else pd.Series()
    wti_may = round((c_wti.iloc[-1]/c_wti.iloc[0]-1)*100, 1) if len(c_wti)>1 else 'N/A'
    sp_may  = round((c_sp.iloc[-1]/c_sp.iloc[0]-1)*100, 1) if len(c_sp)>1 else 'N/A'
    section_kpi([
        ('WTI in May', fmt_pct(wti_may), '#d62728'),
        ('SP500 in May', fmt_pct(sp_may), '#2ca02c'),
        ('Strait capacity (May)', '~2%', '#ff7f0e'),
        ('May Paradox', 'Confirmed', '#ff7f0e'),
    ])
```

**Output:**
```
=== 5-Window Cumulative Returns (%) ===
```
```
                   Pre-event Hormuz Closure False Dawn Paradox Correction  \
Tanker ETF (BWET)    +205.9%         +71.1%      +9.9%              -8.8%   
Gold                  +21.2%          -9.6%      -5.0%              -1.5%   
Defense ETF (ITA)      +9.8%          -8.5%      -5.8%              +8.9%   
Airlines ETF           +0.6%          -3.9%      -8.9%             +13.8%   
S&P 500                +0.3%          +2.3%      +1.2%              +4.8%   
Treasury ETF (TLT)     +4.8%          -3.3%      -1.7%              +0.2%   
WTI Crude             +16.9%         +32.9%     +25.3%             -14.3%   
Exxon (XOM)           +25.2%          -1.4%      +5.4%              -4.3%   

                   Volatile Diplomacy  
Tanker ETF (BWET)              +20.5%  
Gold                            -5.8%  
Defense ETF (ITA)               +1.7%  
Airlines ETF                    +2.1%  
S&P 500                         -2.2%  
Treasury ETF (TLT)              +0.3%  
WTI Crude                       -7.9%  
Exxon (XOM)                     -1.6%
```
```
<IPython.core.display.HTML object>
```

### Chart 21 — Full Timeline Hero Chart

Five regimes, sixteen assets, one crisis. The amber fill during May shows where WTI fell below SP500 even while the strait remained closed — the May Paradox made visible. The red glow on the WTI line signals it as the primary analytical vector throughout.

**Cell 6:**
```python
# ── Chart 21: Five-window normalised price paths — HERO CHART ─────────────────
# Production-grade implementation. This is the dashboard anchor chart.
# Design intent: every visual element makes an analytical argument.
# ─────────────────────────────────────────────────────────────────────────────

import matplotlib.patheffects as pe
import matplotlib.transforms as transforms
from matplotlib.lines import Line2D

chart_assets = ['WTI', 'SP500', 'GOLD', 'ITA', 'BWET', 'TLT']
available    = [a for a in chart_assets if a in prices.columns]

start, end = '2026-01-01', FETCH_END
subset  = prices.loc[start:end, available].dropna(how='all').ffill()
rebased = (subset / subset.iloc[0]) * 100

# ── Figure: 2-panel layout — main chart + regime-bar strip ────────────────────
fig = plt.figure(figsize=(18, 10), facecolor='#0d1117')
gs  = gridspec.GridSpec(2, 1, height_ratios=[11, 1], hspace=0.0)
ax  = fig.add_subplot(gs[0])
ax_bar = fig.add_subplot(gs[1])

for _ax in [ax, ax_bar]:
    _ax.set_facecolor('#0d1117')

# ── Phase bands — drawn before lines ──────────────────────────────────────────
PHASE_BAND_CONFIG = {
    'pre_event':  ('#b0b0b0', 0.055, 'Pre-event'),
    'shock':      ('#d62728', 0.090, 'Shock / Closure'),
    'reopen':     ('#2ca02c', 0.090, 'False Dawn'),
    'correction': ('#ff7f0e', 0.085, 'Paradox Correction'),
    'diplomacy':  ('#4a72b0', 0.085, 'Volatile Diplomacy'),
}

for wname, (ws, we) in EW.items():
    cfg = PHASE_BAND_CONFIG.get(wname)
    if not cfg:
        continue
    color, alpha, label = cfg
    ax.axvspan(pd.Timestamp(ws), pd.Timestamp(we),
               color=color, alpha=alpha, zorder=0, lw=0)
    # Top tick-mark: a 3px colored bar at the very top of each phase
    ax.axvspan(pd.Timestamp(ws), pd.Timestamp(we),
               ymin=0.974, ymax=1.0,
               color=color, alpha=0.80, zorder=2,
               transform=ax.get_xaxis_transform(), lw=0, clip_on=False)

# Event date vertical lines — subtle dashes
for label_ev, date in ED.items():
    ax.axvline(pd.Timestamp(date), color='#444', lw=0.75,
               linestyle=(0, (4, 6)), alpha=0.65, zorder=1)

# ── Decoupling fills ───────────────────────────────────────────────────────────
if 'WTI' in rebased.columns and 'SP500' in rebased.columns:
    # May Paradox fill: WTI fell while SP500 held → show gap inverting
    corr_mask = (
        (rebased.index >= pd.Timestamp('2026-05-01')) &
        (rebased.index <= pd.Timestamp('2026-05-29'))
    )
    r_wti = rebased['WTI'].where(corr_mask)
    r_sp  = rebased['SP500'].where(corr_mask)
    ax.fill_between(rebased.index, r_sp, r_wti,
                    where=(r_wti < r_sp) & corr_mask,
                    color='#ff7f0e', alpha=0.13, zorder=0,
                    label='_nolegend_')

    # Shock decoupling fill: WTI above SP500
    shock_mask = (
        (rebased.index >= pd.Timestamp('2026-02-28')) &
        (rebased.index <= pd.Timestamp('2026-04-16'))
    )
    r_wti_s = rebased['WTI'].where(shock_mask)
    r_sp_s  = rebased['SP500'].where(shock_mask)
    ax.fill_between(rebased.index, r_sp_s, r_wti_s,
                    where=(r_wti_s > r_sp_s) & shock_mask,
                    color='#d62728', alpha=0.07, zorder=0,
                    label='_nolegend_')

# ── Price paths ────────────────────────────────────────────────────────────────
LINE_CFG = {
    'WTI':   {'lw': 3.0, 'zorder': 9, 'alpha': 1.00, 'solid_capstyle': 'round'},
    'SP500': {'lw': 1.9, 'zorder': 7, 'alpha': 0.92},
    'GOLD':  {'lw': 1.7, 'zorder': 6, 'alpha': 0.88},
    'ITA':   {'lw': 1.7, 'zorder': 6, 'alpha': 0.88},
    'BWET':  {'lw': 1.6, 'zorder': 5, 'alpha': 0.82},
    'TLT':   {'lw': 1.4, 'zorder': 4, 'alpha': 0.75},
}

for col in available:
    cfg = LINE_CFG.get(col, {'lw': 1.8, 'zorder': 5, 'alpha': 0.85})
    line_kwargs = {k: v for k, v in cfg.items() if k != 'solid_capstyle'}
    if 'solid_capstyle' in cfg:
        line_kwargs['solid_capstyle'] = cfg['solid_capstyle']
    # Glow effect on WTI only — drawn twice (thick+dim underneath, sharp on top)
    if col == 'WTI':
        ax.plot(rebased.index, rebased[col],
                color=COLORS[col], linewidth=7.0, alpha=0.12, zorder=8)
    ax.plot(rebased.index, rebased[col],
            label=LABELS.get(col, col),
            color=COLORS.get(col, '#ffffff'),
            **line_kwargs)

# Baseline at 100
ax.axhline(100, color='#2d333b', lw=1.0, linestyle='--', alpha=0.8, zorder=1)

# ── Axis limits — set before annotations ──────────────────────────────────────
ax.autoscale(axis='y')
ax.margins(y=0.16)
fig.canvas.draw()
y_lo, y_hi = ax.get_ylim()
y_range = y_hi - y_lo

# ── Phase labels (inline, positioned at top of each band) ─────────────────────
PHASE_LABEL_Y = y_hi - y_range * 0.025
for wname, (ws, we) in EW.items():
    cfg = PHASE_BAND_CONFIG.get(wname)
    if not cfg:
        continue
    color, _, label = cfg
    mid_ts = pd.Timestamp(ws) + (pd.Timestamp(we) - pd.Timestamp(ws)) / 2
    ax.text(mid_ts, PHASE_LABEL_Y,
            label, ha='center', va='top',
            fontsize=6.8, color=color, fontweight='600',
            style='italic', clip_on=True, zorder=10)

# ── Annotations ────────────────────────────────────────────────────────────────
def annotate(ax, text, xy, xytext, color, fontsize=7.8, fontweight='normal'):
    """Consistent annotation style — single call signature, no kwarg conflicts."""
    ax.annotate(
        text, xy=xy, xytext=xytext,
        textcoords='offset points',
        fontsize=fontsize, fontweight=fontweight,
        color=color, clip_on=True,
        arrowprops=dict(arrowstyle='->', color=color, lw=1.2, alpha=0.85,
                        shrinkA=0, shrinkB=4),
        bbox=dict(boxstyle='round,pad=0.38', fc='#0d1117',
                  ec=color, alpha=0.95, lw=1.1),
    )

# 1. WTI peak
if 'WTI' in rebased.columns:
    wti_ser       = rebased['WTI'].dropna()
    pv            = wti_ser.max()
    pd_           = wti_ser.idxmax()
    wti_shock_end = rebased['WTI'].loc[:'2026-04-16'].dropna()
    shock_ret     = round(wti_shock_end.iloc[-1] - 100, 1) if len(wti_shock_end) > 1 else 0
    annotate(ax,
             f'WTI Peak: {pv:.0f}\n{pd_.strftime("%b %d, %Y")}\n+{shock_ret:.0f}pt from Jan 1',
             xy=(pd_, pv), xytext=(-108, 16),
             color=COLORS['WTI'], fontsize=8, fontweight='bold')

# 2. May Paradox — trough of WTI during correction
if 'WTI' in rebased.columns:
    corr_wti = rebased['WTI'].loc['2026-05-01':'2026-05-29'].dropna()
    if not corr_wti.empty:
        trough_v = corr_wti.min()
        trough_d = corr_wti.idxmin()
        drop_pt  = round(corr_wti.iloc[0] - trough_v, 1)
        annotate(ax,
                 f'May Paradox\nStrait: ~2% capacity\nWTI: −{drop_pt:.0f}pt in May\n→ Diplomacy beat supply',
                 xy=(trough_d, trough_v), xytext=(18, -78),
                 color='#ff7f0e')

# 3. BWET freight premium at cutoff
if 'BWET' in rebased.columns:
    bwet_ser = rebased['BWET'].dropna()
    if not bwet_ser.empty:
        bv  = float(bwet_ser.iloc[-1])
        bd  = bwet_ser.index[-1]
        wti_end = float(rebased['WTI'].loc[bd]) if (bd in rebased.index and 'WTI' in rebased.columns) else bv
        spread  = round(bv - wti_end, 1)
        spread_str = f'+{spread:.0f}pt' if spread >= 0 else f'{spread:.0f}pt'
        annotate(ax,
                 f'BWET: {bv:.0f}\n{spread_str} freight premium\nover WTI at cutoff',
                 xy=(bd, bv), xytext=(-145, 10),
                 color=COLORS['BWET'])

# 4. Jun 10 re-escalation
jun10 = pd.Timestamp('2026-06-10')
if jun10 >= pd.Timestamp(start) and 'WTI' in rebased.columns:
    jun10_idx = rebased.index[rebased.index >= jun10]
    if len(jun10_idx):
        jv = float(rebased.loc[jun10_idx[0], 'WTI'])
        annotate(ax,
                 'Jun 10\nRe-escalation',
                 xy=(jun10, jv), xytext=(-60, 44),
                 color='#d62728', fontsize=7)

# 5. Gold margin-call trough during shock
if 'GOLD' in rebased.columns:
    gold_shock = rebased['GOLD'].loc['2026-02-28':'2026-04-16'].dropna()
    if not gold_shock.empty:
        gv = gold_shock.min()
        gd = gold_shock.idxmin()
        annotate(ax,
                 f'Gold: margin-call\nliquidation pattern\n(trough: {gv:.0f})',
                 xy=(gd, gv), xytext=(14, -62),
                 color=COLORS['GOLD'], fontsize=7.2)

# ── Y-axis and grid ────────────────────────────────────────────────────────────
ax.set_xlim(pd.Timestamp(start), pd.Timestamp(end))
ax.set_ylabel('Rebased (Jan 1, 2026 = 100)', fontsize=10, labelpad=9,
              color='#6e7681', fontweight='400')
ax.yaxis.set_major_formatter(mtick.FormatStrFormatter('%.0f'))
ax.yaxis.set_minor_locator(mtick.AutoMinorLocator(2))
ax.grid(axis='y', which='major', alpha=0.20, lw=0.5, color='#30363d')
ax.grid(axis='y', which='minor', alpha=0.08, lw=0.3, color='#30363d')
ax.tick_params(axis='both', which='both', colors='#6e7681', labelsize=8.5)
ax.spines['left'].set_color('#21262d')
ax.spines['bottom'].set_color('#21262d')
style_dates(ax)

# ── Title block ────────────────────────────────────────────────────────────────
fig.text(0.012, 0.975,
         'HORMUZ 2026 — UPDATE 2',
         fontsize=8.5, fontweight='800', color='#d62728',
         fontstyle='normal', va='top', transform=fig.transFigure,
        #  letterSpacing=1.5,
         )
fig.text(0.012, 0.957,
         'Full Timeline: Five Regimes · Sixteen Assets · Jan 1 – Jun 13, 2026',
         fontsize=12.5, fontweight='700', color='#f0f6fc',
         va='top', transform=fig.transFigure)
fig.text(0.012, 0.940,
         'Normalised adjusted-close paths (Base = 100 at Jan 1)  ·  '
         'Ceasefire announced Jun 21–25 — after this cutoff',
         fontsize=8.5, color='#6e7681', va='top', transform=fig.transFigure)

# ── Legend ─────────────────────────────────────────────────────────────────────
asset_handles = [
    Line2D([0], [0], color=COLORS.get(col, '#fff'),
           lw=LINE_CFG.get(col, {}).get('lw', 1.8),
           alpha=LINE_CFG.get(col, {}).get('alpha', 0.9),
           label=LABELS.get(col, col))
    for col in available
]
phase_handles = [
    mpatches.Patch(
        facecolor=PHASE_BAND_CONFIG[k][0],
        alpha=0.45,
        label=PHASE_BAND_CONFIG[k][2],
        edgecolor='none',
    )
    for k in PHASE_ORDER if k in PHASE_BAND_CONFIG
]

legend = ax.legend(
    handles=asset_handles + phase_handles,
    labels=[LABELS.get(col, col) for col in available] +
           [PHASE_BAND_CONFIG[k][2] for k in PHASE_ORDER if k in PHASE_BAND_CONFIG],
    loc='upper left',
    ncol=4,
    fontsize=8.2,
    framealpha=0.0,
    edgecolor='none',
    labelcolor='#c9d1d9',
    handlelength=1.8,
    handleheight=0.9,
    labelspacing=0.45,
    columnspacing=1.2,
    borderpad=0.0,
    bbox_to_anchor=(0.0, 0.98),
)

# ── Bottom regime-bar strip ────────────────────────────────────────────────────
# A thin color-coded timeline bar showing which phase is active — matches dashboard CSS
ax_bar.set_xlim(pd.Timestamp(start), pd.Timestamp(end))
ax_bar.set_ylim(0, 1)
ax_bar.axis('off')

for wname, (ws, we) in EW.items():
    cfg = PHASE_BAND_CONFIG.get(wname)
    if not cfg:
        continue
    color, _, label = cfg
    ax_bar.axvspan(pd.Timestamp(ws), pd.Timestamp(we),
                   color=color, alpha=0.75, lw=0)
    mid_ts = pd.Timestamp(ws) + (pd.Timestamp(we) - pd.Timestamp(ws)) / 2
    ax_bar.text(mid_ts, 0.47, label,
                ha='center', va='center', fontsize=6.5,
                color='#0d1117', fontweight='700',
                clip_on=True, zorder=5)

# ── Snapshot badge ─────────────────────────────────────────────────────────────
fig.text(0.988, 0.014,
         f'Static snapshot · {FETCH_END} · yfinance auto_adjust=True · All figures nominal · '
         'Update 3 will cover the resolution chapter.',
         fontsize=6.5, color='#484f58', ha='right', va='bottom',
         transform=fig.transFigure)

# Snapshot box — top-right corner
fig.text(0.988, 0.975,
         f'UPDATE 2  ·  CUTOFF {FETCH_END}',
         fontsize=6.8, color='#484f58', ha='right', va='top',
         transform=fig.transFigure,
         bbox=dict(boxstyle='round,pad=0.4', fc='#161b22',
                   ec='#30363d', alpha=0.90, lw=0.8))

plt.tight_layout(rect=[0, 0, 1, 0.935])
save_fig(fig, '21_five_window_full_timeline')
plt.show()
```

**Output:**
```
  → c:\Users\User\Desktop\2026-hormuz-blockade-analysis\outputs\update_2\charts\21_five_window_full_timeline.png
```
```
<Figure size 1800x1000 with 2 Axes>
```

## Part 3: Extended Seasonal Baseline — All 16 Assets

Establishes the pre-crisis normal for every asset. BWET (launched May 2023) has only 2024–2025 available — flagged in the `limited_history_flag` column. New assets without a 5-year baseline are clearly distinguished from original assets throughout.

**Cell 7:**
```python
# ── Seasonal baseline ─────────────────────────────────────────────────────────
seasonal = event_study.seasonal_baseline(prices, all_av, years=range(2021, 2027))
base_sum = event_study.baseline_summary(seasonal)

core_display = [a for a in ['WTI','SP500','GOLD','XOM','CVX','ITA','BWET','TLT','XLE'] if a in prices.columns]
ret_piv = seasonal.pivot(index='year', columns='asset', values='cum_return')
ret_piv = ret_piv[[c for c in core_display if c in ret_piv.columns]]
ret_piv.columns = [LABELS.get(c,c) for c in ret_piv.columns]

print('=== Jan-Apr Cumulative Returns by Year (%) — Nominal ===\n')
display(ret_piv.applymap(lambda x: fmt_pct(x) if pd.notna(x) else '—'))
print()
print('=== 2026 vs Baseline Multiple ===')
display(base_sum[['avg_cum_return','2026_cum_return','ret_multiple',
                   'avg_ann_vol','2026_ann_vol','vol_multiple','limited_history']]
        .applymap(lambda x: f'{x:.2f}x' if isinstance(x,(float,int)) and abs(x)<50 else
                  (fmt_pct(x) if isinstance(x,(float,int)) else x)))
```

**Output:**
```
=== Jan-Apr Cumulative Returns by Year (%) — Nominal ===
```
```
     WTI Crude S&P 500    Gold Exxon (XOM) Chevron (CVX) Defense ETF (ITA)  \
year                                                                         
2024    +16.4%   +6.2%  +11.0%      +16.6%         +9.1%             +3.7%   
2025    -20.4%   -5.1%  +24.3%       -0.7%         -6.2%             +8.5%   
2026    +83.3%   +5.1%   +7.0%      +26.7%        +25.2%             -1.5%   

     Tanker ETF (BWET) Treasury ETF (TLT) Energy Sector (XLE)  
year                                                           
2024            +16.0%              -9.4%              +11.2%  
2025            +15.6%              +3.2%               -6.4%  
2026           +772.1%              -0.5%              +31.5%
```
```

=== 2026 vs Baseline Multiple ===
```
```
       avg_cum_return 2026_cum_return ret_multiple avg_ann_vol 2026_ann_vol  \
asset                                                                         
ARAMCO         -8.00x          17.72x       -2.22x      13.02x       16.06x   
BWET           15.82x         +772.1%       48.80x      +53.2%      +130.1%   
CVX             1.42x          25.21x       17.75x      23.50x       22.54x   
DAX             9.64x          -1.01x       -0.10x      16.02x       18.12x   
DXY            -2.57x          -0.35x        0.14x       6.42x        5.81x   
GOLD           17.65x           6.96x        0.39x      14.73x       34.09x   
ITA             6.12x          -1.49x       -0.24x      18.50x       23.11x   
JETS           -6.89x         -10.14x        1.47x      32.75x       32.84x   
NKY             3.53x          14.38x        4.07x      22.50x       27.44x   
SP500           0.54x           5.11x        9.46x      18.31x       13.05x   
TLT            -3.08x          -0.52x        0.17x      13.10x        8.83x   
UNG           -16.02x         -12.11x        0.76x      +58.2%       +73.0%   
VIX            28.16x          16.40x        0.58x     +120.6%      +122.8%   
WTI            -1.99x          +83.3%      -41.86x      25.83x       +67.0%   
XLE             2.44x          31.52x       12.92x      23.14x       20.68x   
XOM             7.97x          26.67x        3.35x      21.80x       26.80x   

       vol_multiple limited_history  
asset                                
ARAMCO        1.23x           0.00x  
BWET          2.44x           1.00x  
CVX           0.96x           0.00x  
DAX           1.13x           0.00x  
DXY           0.90x           0.00x  
GOLD          2.31x           0.00x  
ITA           1.25x           0.00x  
JETS          1.00x           0.00x  
NKY           1.22x           0.00x  
SP500         0.71x           0.00x  
TLT           0.67x           0.00x  
UNG           1.26x           0.00x  
VIX           1.02x           0.00x  
WTI           2.59x           0.00x  
XLE           0.89x           0.00x  
XOM           1.23x           0.00x
```

**Cell 8:**
```python
# ── Chart 02-U2: Seasonal baseline — WTI vs 5-year band ──────────────────────
fig, axes = plt.subplots(1, 2, figsize=(16, 6))
fig.patch.set_facecolor('#0d1117')
fig.suptitle('Jan–Apr Seasonal Baseline: 2021–2025 Band vs 2026'
             '2026 WTI breaks the scale — 6.5× its 5-year average',
             fontsize=12, fontweight='700', color='#f0f6fc', y=1.01)

baseline_yrs = [y for y in range(2021,2026)]
palette = ['#21262d','#30363d','#3d444d','#484f58','#6e7681']

for ax, asset in zip(axes, ['WTI', 'SP500']):
    ax.set_facecolor('#0d1117')
    for i, yr in enumerate(baseline_yrs):
        p = prices.loc[f'{yr}-01-01':f'{yr}-04-30', asset].dropna()
        if p.empty: continue
        s = (p/p.iloc[0]-1)*100
        ax.plot(range(len(s)), s.values, color=palette[i % len(palette)],
                lw=1.2, alpha=0.7, label=str(yr))
    # 2026
    p26 = prices.loc['2026-01-01':'2026-04-30', asset].dropna()
    if not p26.empty:
        s26 = (p26/p26.iloc[0]-1)*100
        ax.plot(range(len(s26)), s26.values, color=COLORS.get(asset,'#d62728'),
                lw=3.0, alpha=1.0, label='2026', zorder=5)
    ax.axhline(0, color='#30363d', lw=0.8, linestyle='--')
    ax.set_xlabel('Trading days (Jan 1 = 0)', fontsize=9, color='#8b949e')
    ax.set_ylabel('Cumulative return (%)', fontsize=9, color='#8b949e')
    ax.yaxis.set_major_formatter(mtick.PercentFormatter(decimals=0))
    ax.set_title(LABELS.get(asset,asset), fontsize=11, fontweight='700', color='#f0f6fc')
    ax.legend(fontsize=8, labelcolor='#c9d1d9', ncol=2)
    ax.grid(alpha=0.2, lw=0.4)

plt.tight_layout()
save_fig(fig, '02_seasonal_baseline_u2')
plt.show()
```

**Output:**
```
  → c:\Users\User\Desktop\2026-hormuz-blockade-analysis\outputs\update_2\charts\02_seasonal_baseline_u2.png
```
```
<Figure size 1600x600 with 2 Axes>
```

## Part 4: Event Window Analysis — All 5 Windows

The May Correction (Paradox Correction) column is the analytical center of Update 2. Read it against the Shock column: the same strait, same physical conditions, opposite price direction. That is the May Paradox in table form.

**Cell 9:**
```python
# ── Full 5-window stats ────────────────────────────────────────────────────────
key_assets = ['WTI','SP500','GOLD','XOM','CVX','ITA','BWET','TLT','XLE','JETS']
key_assets = [a for a in key_assets if a in prices.columns]

def styled_ev_table(window_name):
    sub = ev_stats[ev_stats['window']==window_name].copy()
    sub = sub[sub['asset'].isin(key_assets)].sort_values('cum_return', ascending=False)
    sub['asset'] = sub['asset'].map(lambda x: LABELS.get(x,x))
    return sub[['asset','cum_return','ann_vol','max_dd','n_days']].rename(
        columns={'asset':'Asset','cum_return':'Return %',
                 'ann_vol':'Ann Vol %','max_dd':'Max DD %','n_days':'Days'})

for wname in PHASE_ORDER:
    t = styled_ev_table(wname)
    if t.empty: continue
    print(f'\n━━ {WL.get(wname,wname)} ━━')
    display(t.applymap(lambda x: fmt_pct(x) if isinstance(x,float) else x))
```

**Output:**
```

━━ Pre-event ━━
```
```
                  Asset Return % Ann Vol % Max DD %  Days
11    Tanker ETF (BWET)  +205.9%    +85.6%   -10.0%    39
3           Exxon (XOM)   +25.2%    +27.6%    -5.5%    39
13  Energy Sector (XLE)   +22.5%    +20.6%    -3.8%    39
2                  Gold   +21.2%    +39.2%   -13.1%    39
4         Chevron (CVX)   +21.0%    +20.7%    -5.3%    39
1             WTI Crude   +16.9%    +31.8%    -5.0%    39
10    Defense ETF (ITA)    +9.8%    +19.0%    -7.2%    39
12   Treasury ETF (TLT)    +4.8%     +7.9%    -1.9%    39
6          Airlines ETF    +0.6%    +30.7%    -8.7%    39
0               S&P 500    +0.3%    +11.0%    -2.6%    39
```
```

━━ Hormuz Closure ━━
```
```
                  Asset Return % Ann Vol % Max DD %  Days
27    Tanker ETF (BWET)   +71.1%   +163.1%   -30.6%    33
17            WTI Crude   +32.9%    +91.8%   -19.2%    33
16              S&P 500    +2.3%    +16.3%    -7.8%    33
29  Energy Sector (XLE)    -0.2%    +20.8%   -10.9%    33
20        Chevron (CVX)    -0.8%    +25.8%   -12.4%    33
19          Exxon (XOM)    -1.4%    +27.2%   -13.1%    33
28   Treasury ETF (TLT)    -3.3%    +10.1%    -4.4%    33
22         Airlines ETF    -3.9%    +35.5%   -14.7%    33
26    Defense ETF (ITA)    -8.5%    +26.0%   -15.8%    33
18                 Gold    -9.6%    +31.7%   -17.4%    33
```
```

━━ False Dawn ━━
```
```
                  Asset Return % Ann Vol % Max DD %  Days
33            WTI Crude   +25.3%    +48.3%    -1.7%    10
43    Tanker ETF (BWET)    +9.9%    +94.5%   -13.8%    10
45  Energy Sector (XLE)    +8.4%    +13.7%    -0.4%    10
35          Exxon (XOM)    +5.4%    +17.2%    -1.6%    10
36        Chevron (CVX)    +5.1%    +17.0%    -1.5%    10
32              S&P 500    +1.2%     +9.9%    -0.9%    10
44   Treasury ETF (TLT)    -1.7%     +5.3%    -1.7%    10
34                 Gold    -5.0%    +18.5%    -6.4%    10
42    Defense ETF (ITA)    -5.8%    +26.4%    -8.1%    10
38         Airlines ETF    -8.9%    +25.8%   -10.7%    10
```
```

━━ Paradox Correction ━━
```
```
                  Asset Return % Ann Vol % Max DD %  Days
54         Airlines ETF   +13.8%    +37.9%   -10.1%    20
58    Defense ETF (ITA)    +8.9%    +21.8%    -3.9%    20
48              S&P 500    +4.8%     +9.5%    -2.0%    20
60   Treasury ETF (TLT)    +0.2%     +9.2%    -3.5%    20
50                 Gold    -1.5%    +16.6%    -5.8%    20
52        Chevron (CVX)    -3.4%    +26.4%    -7.5%    20
51          Exxon (XOM)    -4.3%    +31.7%   -10.6%    20
61  Energy Sector (XLE)    -4.3%    +26.1%    -8.2%    20
59    Tanker ETF (BWET)    -8.8%   +113.4%   -29.2%    20
49            WTI Crude   -14.3%    +52.2%   -19.6%    20
```
```

━━ Volatile Diplomacy ━━
```
```
                  Asset Return % Ann Vol % Max DD %  Days
75    Tanker ETF (BWET)   +20.5%    +83.3%    -9.6%    10
70         Airlines ETF    +2.1%    +48.1%    -5.8%    10
74    Defense ETF (ITA)    +1.7%    +36.0%    -2.9%    10
68        Chevron (CVX)    +0.8%    +19.3%    -2.1%    10
77  Energy Sector (XLE)    +0.4%    +21.7%    -2.8%    10
76   Treasury ETF (TLT)    +0.3%     +9.1%    -1.2%    10
67          Exxon (XOM)    -1.6%    +23.2%    -3.9%    10
64              S&P 500    -2.2%    +19.3%    -4.5%    10
66                 Gold    -5.8%    +30.7%    -8.9%    10
65            WTI Crude    -7.9%    +38.5%   -11.6%    10
```

### Chart 30 — 16 Assets × 5 Windows Heatmap

The single-table summary. Read the Paradox Correction column: WTI should be negative while the strait was still closed. That column is the May Paradox made quantitative across all 16 assets.

**Cell 10:**
```python
# ── Chart 30: 16×5 Sector Rotation Heatmap ────────────────────────────────────
import matplotlib.colors as mcolors
from matplotlib.colors import LinearSegmentedColormap

heatmap = event_study.sector_rotation_heatmap(prices, assets=all_av)

hm_plot = heatmap.copy()
hm_plot.index = [LABELS.get(i, i) for i in hm_plot.index]

col_map = {
    'pre_event':  'Pre-event\nJan 1 – Feb 27',
    'shock':      'Shock / Closure\nFeb 28 – Apr 16',
    'reopen':     'False Dawn\nApr 17 – Apr 30',
    'correction': 'Paradox Correction\nMay 1 – May 29',
    'diplomacy':  'Volatile Diplomacy\nJun 1 – Jun 13',
}
hm_plot.columns = [col_map.get(c, c) for c in hm_plot.columns]

# ── Annotation matrix — clean +/- fmt, N/A for missing ────────────────────────
annot = hm_plot.applymap(lambda x: fmt_pct(x) if pd.notna(x) else '—')

# ── Custom colormap: deep blue → near-black → deep red/orange ─────────────────
# Avoids the washed-out center that seaborn diverging_palette produces
cmap_colors = [
    '#1a3a6b',   # deep loss blue
    '#0d1f3c',   # dark mid-blue
    '#0d1117',   # near-black center
    '#4a1010',   # dark mid-red
    '#d62728',   # strong gain red
]
cmap = LinearSegmentedColormap.from_list('hormuz_div', cmap_colors, N=512)

# ── Figure ─────────────────────────────────────────────────────────────────────
fig = plt.figure(figsize=(16, 11), facecolor='#0d1117')
# Reserve top for title block, right for colorbar, bottom for footer
gs = gridspec.GridSpec(
    1, 2,
    width_ratios=[22, 1],
    left=0.19, right=0.93,
    top=0.87, bottom=0.07,
    wspace=0.03,
)
ax     = fig.add_subplot(gs[0, 0])
ax_cb  = fig.add_subplot(gs[0, 1])
ax.set_facecolor('#0d1117')

# ── Determine vmax symmetrically around 0 ─────────────────────────────────────
abs_max = hm_plot.abs().max().max()
vmax = min(abs_max, 120)   # cap at ±120% so extreme WTI doesn't flatten everything

# ── Heatmap ───────────────────────────────────────────────────────────────────
sns.heatmap(
    hm_plot,
    annot=annot, fmt='',
    ax=ax,
    cmap=cmap,
    center=0,
    vmin=-vmax, vmax=vmax,
    linewidths=0.6, linecolor='#0d1117',
    annot_kws={'size': 8.5, 'weight': 'bold'},
    cbar=False,     # manual colorbar in ax_cb
    square=False,
)

# ── Per-cell annotation color: white on dark cells, dark on light cells ────────
for text_obj in ax.texts:
    try:
        val_str = text_obj.get_text().replace('+','').replace('%','').strip()
        if val_str in ('—', '', 'N/A'):
            text_obj.set_color('#484f58')
            continue
        val = float(val_str)
        # Strong colors need white text; near-zero needs muted text
        if abs(val) > 15:
            text_obj.set_color('#f0f6fc')
        elif abs(val) > 5:
            text_obj.set_color('#c9d1d9')
        else:
            text_obj.set_color('#8b949e')
    except (ValueError, AttributeError):
        text_obj.set_color('#8b949e')

# ── Column phase-color header bars (drawn above x-axis tick labels) ────────────
PHASE_COLS = {
    'Pre-event\nJan 1 – Feb 27':           '#b0b0b0',
    'Shock / Closure\nFeb 28 – Apr 16':    '#d62728',
    'False Dawn\nApr 17 – Apr 30':          '#2ca02c',
    'Paradox Correction\nMay 1 – May 29':  '#ff7f0e',
    'Volatile Diplomacy\nJun 1 – Jun 13':  '#4a72b0',
}
n_cols = len(hm_plot.columns)
for col_idx, col_name in enumerate(hm_plot.columns):
    color = PHASE_COLS.get(col_name, '#555')
    # Thin colored bar at top of each column — drawn in axis coords
    ax.add_patch(mpatches.FancyArrowPatch(
        (col_idx, len(hm_plot)),
        (col_idx + 1, len(hm_plot)),
        arrowstyle='-', color=color, lw=0,
        zorder=5,
    ))
    ax.axvspan(col_idx, col_idx + 1,
               ymin=1.0, ymax=1.025,
               color=color, alpha=0.85, clip_on=False, zorder=4)

# ── Row group dividers — separate original 10 from new 6 ──────────────────────
# Find the row index of the first new asset
new_asset_labels = set(LABELS.get(a, a) for a in NEW_ASSETS_U2)
row_labels = list(hm_plot.index)
first_new  = next((i for i, lbl in enumerate(row_labels) if lbl in new_asset_labels), None)
if first_new is not None:
    ax.axhline(first_new, color='#ff7f0e', lw=1.2,
               linestyle=(0, (6, 4)), alpha=0.65, zorder=5)
    ax.text(-0.35, first_new - 0.15, '← Update 2',
            ha='right', va='bottom', fontsize=6.8, color='#ff7f0e',
            style='italic', transform=ax.transData, clip_on=False)
    ax.text(-0.35, first_new + 0.15, '← Original',
            ha='right', va='top', fontsize=6.8, color='#6e7681',
            style='italic', transform=ax.transData, clip_on=False)

# ── Axis styling ───────────────────────────────────────────────────────────────
ax.tick_params(axis='y', labelrotation=0, labelsize=8.8, colors='#c9d1d9',
               length=0, pad=6)
ax.tick_params(axis='x', labelsize=8.2, colors='#8b949e',
               length=0, pad=5)
ax.set_xlabel('')
ax.set_ylabel('')

# Highlight WTI row label — it's the primary finding anchor
for tick_label in ax.get_yticklabels():
    if tick_label.get_text() == LABELS.get('WTI', 'WTI Crude'):
        tick_label.set_color(COLORS['WTI'])
        tick_label.set_fontweight('700')
    if tick_label.get_text() in (LABELS.get(a, a) for a in NEW_ASSETS_U2):
        tick_label.set_color('#ff7f0e')

# ── Manual colorbar ────────────────────────────────────────────────────────────
norm = mcolors.TwoSlopeNorm(vmin=-vmax, vcenter=0, vmax=vmax)
sm   = plt.cm.ScalarMappable(cmap=cmap, norm=norm)
sm.set_array([])
cb = fig.colorbar(sm, cax=ax_cb, orientation='vertical')
cb.ax.tick_params(colors='#6e7681', labelsize=7.5, length=3)
cb.set_label('Cumulative Return %', color='#6e7681', fontsize=7.5, labelpad=8)
cb.outline.set_edgecolor('#21262d')
# Tick labels as +/- pct
cb.set_ticks([-vmax, -vmax/2, 0, vmax/2, vmax])
cb.set_ticklabels([fmt_pct(-vmax), fmt_pct(-vmax/2), '0%',
                   fmt_pct(vmax/2), fmt_pct(vmax)])

# ── Title block ────────────────────────────────────────────────────────────────
fig.text(0.012, 0.975,
         'SECTOR ROTATION MATRIX  ·  UPDATE 2',
         fontsize=8, fontweight='800', color='#ff7f0e',
         va='top', transform=fig.transFigure)
fig.text(0.012, 0.958,
         '16 Assets × 5 Windows — Complete Return Matrix',
         fontsize=13, fontweight='700', color='#f0f6fc',
         va='top', transform=fig.transFigure)
fig.text(0.012, 0.940,
         'Sorted by shock-window return  ·  Orange row labels = Update 2 additions  ·  '
         'Red label = WTI (primary shock vector)  ·  '
         'Dashed line separates original 10 from 6 new assets',
         fontsize=7.8, color='#6e7681',
         va='top', transform=fig.transFigure)

# Three reading guides across the top — one per key finding
guides = [
    (0.33, '#d62728', 'Shock column: only WTI positive'),
    (0.55, '#ff7f0e', 'Correction column: WTI negative while strait closed — the Paradox'),
    (0.78, '#4a72b0', 'Diplomacy column: re-escalation effect'),
]
for xpos, color, text in guides:
    fig.text(xpos, 0.895, text,
             fontsize=7, color=color, ha='center', va='top',
             transform=fig.transFigure,
             bbox=dict(boxstyle='round,pad=0.28', fc='#0d1117',
                       ec=color, alpha=0.80, lw=0.8))

# ── Footer ─────────────────────────────────────────────────────────────────────
fig.text(0.012, 0.022,
         'Sorted by Shock window return (descending).  '
         'BWET: launched May 2023 — limited baseline (2 years vs 5).  '
         'ARAMCO (2222.SR): Tadawul forward-filled to US calendar.  '
         'All returns nominal, no CPI adjustment.',
         fontsize=6.5, color='#484f58', va='bottom', transform=fig.transFigure)
fig.text(0.988, 0.022,
         f'Static snapshot · cutoff {FETCH_END}',
         fontsize=6.5, color='#484f58', ha='right', va='bottom',
         transform=fig.transFigure)

save_fig(fig, '30_16x5_full_heatmap')
plt.show()
```

**Output:**
```
  → c:\Users\User\Desktop\2026-hormuz-blockade-analysis\outputs\update_2\charts\30_16x5_full_heatmap.png
```
```
<Figure size 1600x1100 with 2 Axes>
```

## Part 5: The Paradox Correction — When Supply Stopped Mattering

**Setup:** Strait still at ~2% of normal transit capacity throughout May. No new barrels started moving. The physical disruption that drove WTI to +32.9% in March–April was structurally unchanged.

**What happened:** WTI dropped ~20% in May. Forward markets were pricing the barrel that *would* eventually flow through a re-opened strait, not the barrel that couldn't move today. This is the regime shift — from **scarcity pricing** to **expectations pricing**.

**The WTI/Gold ratio is the cleanest signal.** It peaked on April 7 (first ceasefire announcement) and declined through May even as the strait remained closed. That peak is the exit signal for the Long WTI / Short XOM pair trade — not the official ceasefire, which came later with more noise.

| WTI/Gold Rising | WTI/Gold Falling |
|-----------------|-----------------|
| Supply shock dominant | Diplomacy dominant |
| Oil outpaces gold | Gold holds / Oil corrects |
| Physical scarcity pricing | Expectations pricing |
| Long WTI pair: entry signal | Long WTI pair: exit signal |

**Cell 11:**
```python
# ── WTI/Gold ratio analysis ────────────────────────────────────────────────────
og_ratio   = event_study.oil_gold_ratio_stats(prices, end=FETCH_END)
regime_s   = event_study.window_regime_summary(prices, og_ratio)

print('=== WTI/Gold Ratio — Per-Window Change ===\n')
for k in [f'{w}_change_%' for w in PHASE_ORDER]:
    v = og_ratio.get(k)
    wname = k.replace('_change_%','')
    if v is not None:
        direction = '↑ Supply shock' if v > 0 else '↓ Diplomacy / fear'
        print(f'  {WL.get(wname, wname):<25}: {fmt_pct(v):>8}  {direction}')

print(f'\n  Peak date : {og_ratio.get("peak_ratio_date")}  ← pair trade exit signal')
print(f'  Shock     : {og_ratio.get("regime_interp")}')
print(f'  Correction: {og_ratio.get("correction_regime")}')

print('\n=== Window Regime Classification ===')
if not regime_s.empty:
    display(regime_s[['window_label','wti_ret_%','og_ratio_chg_%','wti_sp500_corr','regime']]
            .applymap(lambda x: fmt_pct(x) if isinstance(x,float) else x))
```

**Output:**
```
=== WTI/Gold Ratio — Per-Window Change ===

  Pre-event                :    -3.6%  ↓ Diplomacy / fear
  Hormuz Closure           :   +47.1%  ↑ Supply shock
  False Dawn               :   +31.9%  ↑ Supply shock
  Paradox Correction       :   -13.0%  ↓ Diplomacy / fear
  Volatile Diplomacy       :    -2.2%  ↓ Diplomacy / fear

  Peak date : 2026-04-07  ← pair trade exit signal
  Shock     : Supply shock dominant. Oil outpaced gold. Markets priced scarcity, not fear.
  Correction: Diplomacy decoupling confirmed: WTI/Gold fell in May even as strait remained at ~2% transit capacity. Forward expectations (peace deal probability) overrode physical supply.

=== Window Regime Classification ===
```
```
         window_label wti_ret_% og_ratio_chg_% wti_sp500_corr           regime
0           Pre-event    +16.9%          -3.6%            N/A        PRE_EVENT
1      Hormuz Closure    +32.9%         +47.1%          -0.6%     SUPPLY_SHOCK
2          False Dawn    +25.3%         +31.9%          -0.7%     SUPPLY_SHOCK
3  Paradox Correction    -14.3%         -13.0%          -0.6%  DIPLOMACY_BREAK
4  Volatile Diplomacy     -7.9%          -2.2%          -0.1%       RESOLUTION
```

**Cell 12:**
```python
# ── Forward expectations proxy ─────────────────────────────────────────────────
fwd = event_study.forward_expectations_proxy(prices)
if not fwd.empty:
    if hasattr(fwd, 'attrs') and 'summary' in fwd.attrs:
        s = fwd.attrs['summary']
        section_kpi([
            ('Diplomacy-day avg WTI', fmt_pct(s['diplomacy_days_avg_wti_%']), '#ff7f0e'),
            ('Non-diplomacy avg WTI', fmt_pct(s['non_diplomacy_days_avg_wti_%']), '#6e7681'),
            ('Differential', fmt_pct(s['differential_%']), '#d62728'),
        ])
        note(s['caveat'])
    print('Diplomacy vs non-diplomacy days (May–Jun window):')
    display(fwd.groupby('is_diplomacy')['wti_return_%'].agg(['mean','std','count'])
            .round(3).rename(index={0:'Non-diplomacy days',1:'Diplomacy days'}))
```

**Output:**
```
<IPython.core.display.HTML object>
```
```
<IPython.core.display.HTML object>
```
```
Diplomacy vs non-diplomacy days (May–Jun window):
```
```
                     mean    std  count
is_diplomacy                           
Non-diplomacy days -0.641  3.811     21
Diplomacy days     -0.384  2.709      8
```

**Cell 13:**
```python
# ── Chart 28: WTI/Gold ratio — full timeline ──────────────────────────────────
ratio = og_ratio.get('ratio_series')
if ratio is not None and not ratio.empty:
    ratio_ma = ratio.rolling(10).mean()

    fig, (ax1, ax2) = plt.subplots(2, 1, figsize=(16, 9),
                                    gridspec_kw={'height_ratios':[3,1.1]})
    fig.patch.set_facecolor('#0d1117')
    for ax in [ax1, ax2]: ax.set_facecolor('#0d1117')

    add_bands(ax1, label_top=True, fontsize=6.8)
    ax1.fill_between(ratio.index, 100, ratio.values,
                     where=ratio.values>100, color='#d62728', alpha=0.10)
    ax1.fill_between(ratio.index, 100, ratio.values,
                     where=ratio.values<100, color='#bcbd22', alpha=0.10)
    ax1.plot(ratio.index, ratio.values, color='#ff7f0e', lw=1.0, alpha=0.45)
    ax1.plot(ratio_ma.index, ratio_ma.values, color='#ff7f0e', lw=2.8,
             label='WTI/Gold ratio (10-day MA)')
    ax1.axhline(100, color='#333', lw=0.9, linestyle='--', alpha=0.7)

    # Peak annotation
    pk_date = og_ratio.get('peak_ratio_date')
    if pk_date:
        try:
            pts = pd.Timestamp(pk_date)
            pv  = float(ratio.loc[pts]) if pts in ratio.index else ratio.max()
            ax1.annotate(
                f'Peak: {pv:.0f}\n→ Pair trade exit signal\n(Apr 7 ceasefire announcement)',
                xy=(pts, pv), xytext=(-115, -50), textcoords='offset points',
                fontsize=8, color='#ff7f0e',
                arrowprops=dict(arrowstyle='->', color='#ff7f0e', lw=1.3, shrinkB=4),
                bbox=dict(boxstyle='round,pad=0.4', fc='#0d1117', ec='#ff7f0e', alpha=0.95, lw=1.1))
        except: pass

    # May paradox annotation on ratio
    may_mid = pd.Timestamp('2026-05-15')
    mi = ratio.index[ratio.index >= may_mid]
    if len(mi):
        mv = float(ratio.loc[mi[0]])
        ax1.annotate('May: ratio fell\nstrait still closed\n→ Diplomacy pricing',
                     xy=(mi[0], mv), xytext=(20, 28), textcoords='offset points',
                     fontsize=7.5, color='#ff7f0e',
                     arrowprops=dict(arrowstyle='->', color='#ff7f0e', lw=1.0, shrinkB=3),
                     bbox=dict(boxstyle='round,pad=0.3', fc='#0d1117', ec='#ff7f0e', alpha=0.90, lw=1.0))

    ax1.set_xlim(ratio.index[0], ratio.index[-1])
    ax1.set_ylabel('WTI/Gold Ratio (Jan 1, 2026 = 100)', fontsize=9.5, color='#6e7681')
    ax1.set_title('WTI/Gold Ratio — The Regime Discriminator\n'
                  'Rising = Supply Shock Dominant  ·  Falling = Diplomacy / Expectations Dominant',
                  fontsize=12, fontweight='700', color='#f0f6fc', pad=10)
    ax1.legend(fontsize=9, labelcolor='#c9d1d9')
    ax1.grid(alpha=0.2, lw=0.4); style_dates(ax1)

    # Bar chart per window
    w_vals = [(wname, og_ratio.get(f'{wname}_change_%', 0) or 0) for wname in PHASE_ORDER]
    w_vals = [(w,v) for w,v in w_vals if v is not None]
    bar_c  = ['#d62728' if v>0 else '#bcbd22' for _,v in w_vals]
    bars   = ax2.bar([WL.get(w,w) for w,_ in w_vals],
                     [v for _,v in w_vals], color=bar_c, alpha=0.85,
                     edgecolor='#0d1117', width=0.55)
    for bar, (_,v) in zip(bars, w_vals):
        ax2.text(bar.get_x()+bar.get_width()/2, v+(0.8 if v>=0 else -2),
                 fmt_pct(v), ha='center', va='bottom' if v>=0 else 'top',
                 fontsize=8.5, fontweight='bold', color='#f0f6fc')
    ax2.axhline(0, color='#333', lw=0.8)
    ax2.set_ylabel('Window change %', fontsize=8.5, color='#6e7681')
    ax2.set_title('WTI/Gold Change Per Window', fontsize=9.5, color='#c9d1d9')
    ax2.grid(axis='y', alpha=0.2, lw=0.4)

    plt.tight_layout()
    save_fig(fig, '28_oil_gold_ratio_full_timeline')
    plt.show()
```

**Output:**
```
  → c:\Users\User\Desktop\2026-hormuz-blockade-analysis\outputs\update_2\charts\28_oil_gold_ratio_full_timeline.png
```
```
<Figure size 1600x900 with 2 Axes>
```

## Part 6: June 10 Re-Escalation — Second Shock Test

**The question:** By June 10, the market had been living with the Hormuz crisis for 102 days. Did the second escalation trigger the same magnitude response as February 28?

**Market learning hypothesis:** If the June 10 shock is smaller in magnitude or faster to reprice, it suggests the market had internalised a playbook. Oil up → equities down → defense up. If similar magnitude, no learning occurred — each escalation is treated as fresh information.

**Escalation Replay** shows 5-day cumulative return arcs for both shock events side-by-side across key assets. The pattern comparison is the finding.

**Cell 14:**
```python
# ── First-day reactions across all 6 events ───────────────────────────────────
fd_assets = ['WTI','SP500','GOLD','XOM','ITA','JETS','TLT']
fd_assets = [a for a in fd_assets if a in prices.columns]
first_day = event_study.first_day_reaction(prices, ED, fd_assets)

print('=== First-Day Reactions — All 6 Events ===\n')
piv_fd = first_day.pivot(index='asset', columns='event', values='day1_return_%')
piv_fd.index = [LABELS.get(i,i) for i in piv_fd.index]
display(piv_fd.applymap(lambda x: fmt_pct(x) if pd.notna(x) else '—'))

# Days to price comparison
for event_key in ['Op. Epic Fury (Feb 28)', 'Iran Re-escalation (Jun 10)']:
    date = ED.get(event_key, '')
    if date:
        d_wti = event_study.days_to_price_event(prices, 'WTI',   date)
        d_sp  = event_study.days_to_price_event(prices, 'SP500', date)
        print(f'\n{event_key}:')
        print(f'  WTI days to reprice   : {d_wti}')
        print(f'  SP500 days to reprice : {d_sp}')
```

**Output:**
```
=== First-Day Reactions — All 6 Events ===
```
```
event              Ceasefire (Apr 7) Hormuz Open (Apr 17)  \
Gold                           +0.0%                +1.5%   
Defense ETF (ITA)              -0.8%                +1.3%   
Airlines ETF                   -0.8%                +4.8%   
S&P 500                        +0.1%                +1.2%   
Treasury ETF (TLT)             -0.0%                +0.9%   
WTI Crude                      +0.5%               -11.4%   
Exxon (XOM)                    +0.3%                -3.6%   

event              Iran Re-escalation (Jun 10) Lebanon CF (Apr 16)  \
Gold                                     -3.6%               -0.3%   
Defense ETF (ITA)                        -2.4%               -2.1%   
Airlines ETF                             -4.6%               -1.4%   
S&P 500                                  -1.6%               +0.3%   
Treasury ETF (TLT)                       -0.3%               -0.6%   
WTI Crude                                +2.1%               +3.7%   
Exxon (XOM)                              +1.1%               +2.0%   

event              May Correction Start Op. Epic Fury (Feb 28)  
Gold                              +0.3%                   0.0%  
Defense ETF (ITA)                 -1.1%                   0.0%  
Airlines ETF                      +1.3%                   0.0%  
S&P 500                           +0.3%                   0.0%  
Treasury ETF (TLT)                +0.4%                   0.0%  
WTI Crude                         -3.0%                   0.0%  
Exxon (XOM)                       -1.0%                   0.0%
```
```

Op. Epic Fury (Feb 28):
  WTI days to reprice   : 0
  SP500 days to reprice : 0

Iran Re-escalation (Jun 10):
  WTI days to reprice   : 20
  SP500 days to reprice : 2
```

**Cell 15:**
```python
# ── Escalation replay ─────────────────────────────────────────────────────────
esc = event_study.escalation_replay(prices, assets=fd_assets)

# Chart 27: Feb 28 vs Jun 10 side-by-side
key_events = [e for e in ['Op. Epic Fury (Feb 28)', 'Iran Re-escalation (Jun 10)']
              if e in esc['event'].unique()]

fig, axes = plt.subplots(1, max(2,len(key_events)), figsize=(16, 7), sharey=False)
fig.patch.set_facecolor('#0d1117')
fig.suptitle('Escalation Replay — Feb 28 vs Jun 10: Did Markets Learn the Playbook?\n'
             'Smaller magnitude on Jun 10 = market learning · Same magnitude = each event treated as fresh',
             fontsize=12, fontweight='700', color='#f0f6fc', y=1.01)

EC = {'Op. Epic Fury (Feb 28)':'#d62728', 'Iran Re-escalation (Jun 10)':'#ff7f0e'}

for ax, ev in zip(axes, key_events):
    ax.set_facecolor('#0d1117')
    sub = esc[esc['event'] == ev]
    # Plot each asset
    for asset in ['WTI','SP500','GOLD','ITA']:
        if asset not in prices.columns: continue
        a_sub = sub[sub['asset']==asset]
        if a_sub.empty: continue
        ax.plot(a_sub['day'], a_sub['cum_return_%'],
                label=LABELS.get(asset,asset), color=COLORS.get(asset,'#888'),
                lw=2.8 if asset=='WTI' else 1.8,
                marker='o', markersize=4.5, alpha=0.90)
    ax.axhline(0, color='#333', lw=0.8, linestyle='--')
    ax.set_xlabel('Trading days after event', fontsize=9, color='#8b949e')
    ax.set_ylabel('Cumulative return (%)', fontsize=9, color='#8b949e')
    ax.yaxis.set_major_formatter(mtick.PercentFormatter(decimals=1))
    ax.set_title(ev, fontsize=10.5, fontweight='700', color=EC.get(ev,'#f0f6fc'))
    ax.legend(fontsize=8.5, labelcolor='#c9d1d9')
    ax.set_xticks(range(1,6))
    ax.grid(alpha=0.2, lw=0.4)

plt.tight_layout()
save_fig(fig, '27_escalation_replay')
plt.show()
```

**Output:**
```
  → c:\Users\User\Desktop\2026-hormuz-blockade-analysis\outputs\update_2\charts\27_escalation_replay.png
```
```
<Figure size 1600x700 with 2 Axes>
```

## Part 7: Defense as an Asset Class — ITA

**The Backlog Thesis:** Defense revenue is driven by multi-year government contracts, not quarterly conflict intensity. LMT, RTX, and NOC backlogs extend to 2028–2030. A ceasefire in April 2026 doesn't cancel an F-35 order placed in 2024.

**What this predicts:**
- ITA gains on day 1 of Feb 28 (war premium priced in) ✓
- ITA does NOT crater on ceasefire signals the same way oil does
- ITA abnormal return stays positive through the May correction
- ITA premium only compresses when sustained multi-year peace reduces new orders

**This is exactly the opposite of oil** — oil reprices on supply signals within days; defense reprices on order-book signals over quarters.

**Cell 16:**
```python
# ── Defense premium analysis ──────────────────────────────────────────────────
def_premium = event_study.defense_premium(prices)
print('=== ITA Defense Premium — OLS Abnormal Return vs S&P 500 ===\n')
if not def_premium.empty:
    cols = ['window','actual_ret_%','expected_ret_%','abnormal_ret_%','beta','r2','defense_interpretation']
    cols = [c for c in cols if c in def_premium.columns]
    display(def_premium[cols].applymap(lambda x: fmt_pct(x) if isinstance(x,float) else x))
    if 'backlog_note' in def_premium.columns:
        callout(def_premium.iloc[0]['backlog_note'], title='Backlog Thesis', color='#1f77b4')
else:
    note('ITA not in price data.')
```

**Output:**
```
=== ITA Defense Premium — OLS Abnormal Return vs S&P 500 ===
```
```
       window actual_ret_% expected_ret_% abnormal_ret_%   beta     r2  \
0       shock        -8.5%          +7.4%         -15.9%  +0.8%  +0.2%   
1      reopen        -5.8%          +2.5%          -8.2%  +0.8%  +0.2%   
2  correction        +8.9%          +7.1%          +1.8%  +0.8%  +0.2%   
3   diplomacy        +1.7%          -0.2%          +1.9%  +0.8%  +0.2%   

                              defense_interpretation  
0  No war premium: defense tracked market, not es...  
1      Defense premium closed on resolution signals.  
2  Muted correction response: backlog vs peace si...  
3  Gradual giveback: defense slowly repriced towa...
```
```
<IPython.core.display.HTML object>
```

**Cell 17:**
```python
# ── Chart 22: ITA full analysis ────────────────────────────────────────────────
if 'ITA' in prices.columns:
    start_c, end_c = '2026-01-01', FETCH_END
    sub_it = prices.loc[start_c:end_c, ['ITA','SP500','WTI']].dropna(how='all').ffill()
    reb_it = (sub_it / sub_it.iloc[0]) * 100
    rb_it  = event_study.rolling_beta_series(prices, assets=['ITA'], benchmark='SP500',
                                              start=start_c, end=end_c)

    fig, (ax1, ax2) = plt.subplots(2, 1, figsize=(15, 9),
                                    gridspec_kw={'height_ratios':[2.2,1]})
    fig.patch.set_facecolor('#0d1117')
    for ax in [ax1,ax2]: ax.set_facecolor('#0d1117')

    add_bands(ax1, label_top=True, fontsize=6.8)
    for col in ['ITA','SP500','WTI']:
        if col not in reb_it.columns: continue
        lw  = 3.0 if col=='ITA' else 1.8
        ax1.plot(reb_it.index, reb_it[col], label=LABELS.get(col,col),
                 color=COLORS.get(col,'#aaa'), lw=lw, alpha=0.90)
    ax1.axhline(100, color='#2d333b', lw=0.8, linestyle='--', alpha=0.7)

    # Annotation: ITA vs SP500 at correction end
    corr_end = pd.Timestamp('2026-05-29')
    ce_idx = reb_it.index[reb_it.index <= corr_end]
    if len(ce_idx) and 'ITA' in reb_it.columns and 'WTI' in reb_it.columns:
        ita_v = float(reb_it.loc[ce_idx[-1], 'ITA'])
        wti_v = float(reb_it.loc[ce_idx[-1], 'WTI'])
        ann(ax1, f'End of May: ITA={ita_v:.0f}\nWTI={wti_v:.0f}\nBacklog held premium',
            xy=(ce_idx[-1], ita_v), xytext=(-110, 20),
            color='#1f77b4')

    style_dates(ax1); ax1.set_xlim(reb_it.index[0], reb_it.index[-1])
    ax1.set_ylabel('Rebased (Jan 1 = 100)', fontsize=9, color='#6e7681')
    ax1.set_title('Defense ETF (ITA) — Backlog Trade, Not War Trade\n'
                  'ITA should hold premium through May Correction even as oil corrects 20%',
                  fontsize=12, fontweight='700', color='#f0f6fc')
    ax1.legend(fontsize=9, labelcolor='#c9d1d9'); ax1.grid(alpha=0.2, lw=0.4)

    if not rb_it.empty and 'ITA' in rb_it.columns:
        add_bands(ax2, label_top=False)
        ax2.plot(rb_it.index, rb_it['ITA'], color=COLORS.get('ITA','#1f77b4'),
                 lw=2.2, label='ITA beta vs SP500 (21d rolling)')
        ax2.axhline(1.0, color='#444', lw=0.8, linestyle='--', alpha=0.7, label='beta=1')
        ax2.axhline(0.0, color='#2d333b', lw=0.5, linestyle=':')
        style_dates(ax2); ax2.set_xlim(reb_it.index[0], reb_it.index[-1])
        ax2.set_ylabel('Rolling Beta vs SP500', fontsize=9, color='#6e7681')
        ax2.set_title('ITA Rolling 21-Day Beta — Elevated Market Sensitivity Through Correction?',
                      fontsize=10, color='#c9d1d9')
        ax2.legend(fontsize=8.5, labelcolor='#c9d1d9'); ax2.grid(alpha=0.2, lw=0.4)

    fig.text(0.01,-0.02,
             'ITA = iShares US Aerospace & Defense ETF · Beta estimated on 39 pre-event days · '
             'Abnormal returns are directional bounds, not precise estimates.',
             fontsize=7, color='#484f58')
    plt.tight_layout()
    save_fig(fig, '22_defense_ita_analysis')
    plt.show()
else:
    note('ITA not in price data.')
```

**Output:**
```
  → c:\Users\User\Desktop\2026-hormuz-blockade-analysis\outputs\update_2\charts\22_defense_ita_analysis.png
```
```
<Figure size 1500x900 with 2 Axes>
```

## Part 8: Freight Rates vs Oil — The Third Decoupling

Three decouplings define this study:

1. **WTI decoupled from energy equities** (XOM fell while oil rallied) — Update 1
2. **WTI decoupled from forward expectations** (oil fell while strait was closed) — Update 2, May Paradox
3. **BWET decoupled from WTI** (freight futures outperformed the commodity they ship) — Update 2, new

The third decoupling: BWET returned ~+100% YTD while WTI returned ~+87%. Tanker scarcity was more acute than oil scarcity because: war-risk insurance premiums on Gulf routes, vessel rerouting around the Cape of Good Hope (+10-14 days), and reduced tanker availability on alternative routes drove VLCC day rates above and beyond the commodity price spike.

> **Caveat:** BWET is a $24.9M AUM futures ETF subject to contango roll decay. It's an analytical signal, not a trading recommendation.

**Cell 18:**
```python
# ── Freight/oil spread ────────────────────────────────────────────────────────
freight_s = event_study.freight_oil_spread(prices)
print('=== Freight/Oil Spread — BWET vs WTI (Third Decoupling) ===\n')
if not freight_s.empty:
    display(freight_s[['window_label','bwet_ret_%','wti_ret_%','spread_%','spread_interp']]
            .applymap(lambda x: fmt_pct(x) if isinstance(x,float) else x))
    note(freight_s.iloc[0]['caveat'])
else:
    note('BWET not in price data.')
```

**Output:**
```
=== Freight/Oil Spread — BWET vs WTI (Third Decoupling) ===
```
```
         window_label bwet_ret_% wti_ret_% spread_%  \
0           Pre-event    +205.9%    +16.9%  +189.0%   
1      Hormuz Closure     +71.1%    +32.9%   +38.2%   
2          False Dawn      +9.9%    +25.3%   -15.4%   
3  Paradox Correction      -8.8%    -14.3%    +5.5%   
4  Volatile Diplomacy     +20.5%     -7.9%   +28.4%   

                                       spread_interp  
0  Tanker scarcity premium: freight outpaced comm...  
1  Tanker scarcity premium: freight outpaced comm...  
2  Freight normalising: oil scarcity dominates ov...  
3  Tanker scarcity premium: freight outpaced comm...  
4  Tanker scarcity premium: freight outpaced comm...
```
```
<IPython.core.display.HTML object>
```

**Cell 19:**
```python
# ── Chart 23: Freight vs Oil ──────────────────────────────────────────────────
if 'BWET' in prices.columns:
    s_f = prices.loc['2026-01-01':FETCH_END, ['BWET','WTI']].dropna(how='all').ffill()
    r_f = (s_f / s_f.iloc[0]) * 100

    fig, (ax1, ax2) = plt.subplots(2, 1, figsize=(15, 9),
                                    gridspec_kw={'height_ratios':[2.5,1.2]})
    fig.patch.set_facecolor('#0d1117')
    for ax in [ax1,ax2]: ax.set_facecolor('#0d1117')

    add_bands(ax1, label_top=True, fontsize=6.8)
    if 'BWET' in r_f.columns and 'WTI' in r_f.columns:
        aligned = r_f[['BWET','WTI']].dropna()
        ax1.fill_between(aligned.index, aligned['WTI'], aligned['BWET'],
                         where=aligned['BWET']>aligned['WTI'],
                         color='#ff7f0e', alpha=0.13, label='Freight premium over WTI')
    for col in ['BWET','WTI']:
        if col not in r_f.columns: continue
        lw = 2.8 if col=='BWET' else 2.2
        ax1.plot(r_f.index, r_f[col], label=LABELS.get(col,col),
                 color=COLORS.get(col,'#aaa'), lw=lw, alpha=0.90)

    ax1.axhline(100, color='#2d333b', lw=0.8, linestyle='--', alpha=0.7)
    # Endpoint annotation
    if 'BWET' in r_f.columns and 'WTI' in r_f.columns:
        bv = float(r_f['BWET'].dropna().iloc[-1])
        wv = float(r_f['WTI'].dropna().iloc[-1])
        ann(ax1, f'BWET: {bv:.0f}\nWTI: {wv:.0f}\nSpread: +{bv-wv:.0f}pt',
            xy=(r_f.index[-1], bv), xytext=(-135, 10),
            color='#ff7f0e')

    style_dates(ax1); ax1.set_xlim(r_f.index[0], r_f.index[-1])
    ax1.set_ylabel('Rebased (Jan 1 = 100)', fontsize=9, color='#6e7681')
    ax1.set_title('BWET vs WTI — The Third Decoupling\n'
                  'Freight futures outpaced the commodity they ship · Orange fill = freight crisis premium',
                  fontsize=12, fontweight='700', color='#f0f6fc')
    ax1.legend(fontsize=9, labelcolor='#c9d1d9'); ax1.grid(alpha=0.2, lw=0.4)

    if not freight_s.empty:
        bars_v = freight_s['spread_%'].values
        bars_c = ['#ff7f0e' if v>0 else '#d62728' for v in bars_v]
        bars   = ax2.bar(freight_s['window_label'], bars_v,
                         color=bars_c, alpha=0.85, edgecolor='#0d1117', width=0.55)
        for bar, v in zip(bars, bars_v):
            ax2.text(bar.get_x()+bar.get_width()/2, v+(0.4 if v>=0 else -0.9),
                     fmt_pct(v), ha='center', va='bottom' if v>=0 else 'top',
                     fontsize=9, fontweight='bold', color='#f0f6fc')
        ax2.axhline(0, color='#333', lw=0.8)
        ax2.set_ylabel('Freight premium %\n(BWET − WTI)', fontsize=8.5, color='#6e7681')
        ax2.grid(axis='y', alpha=0.2, lw=0.4)
        ax2.set_title('BWET−WTI Spread Per Window — Tanker vs Commodity Scarcity',
                      fontsize=10, color='#c9d1d9')

    fig.text(0.01,-0.02,
             'BWET = Breakwave Tanker Shipping ETF · AUM $24.9M · Futures-based — contango roll applies · '
             'Launched May 2023 (2-year baseline only) · Analytical signal, not a trading recommendation.',
             fontsize=7, color='#484f58')
    plt.tight_layout()
    save_fig(fig, '23_freight_oil_third_decoupling')
    plt.show()
else:
    note('BWET not in price data.')
```

**Output:**
```
  → c:\Users\User\Desktop\2026-hormuz-blockade-analysis\outputs\update_2\charts\23_freight_oil_third_decoupling.png
```
```
<Figure size 1500x900 with 2 Axes>
```

## Part 9: Bond Market — TLT Safe Haven Test

Update 1 found that gold failed as a safe haven (margin-call liquidation). Update 2 adds TLT (20+ Year Treasury ETF) to test whether bonds complete the safe-haven failure trifecta.

**Supply shocks and bonds:** In financial crises (2008, 2020), flight-to-quality drives yields down and TLT up. In supply shocks, inflation expectations rise, pushing yields up and TLT *down*. The 2026 Hormuz closure should have been a headwind for long-duration bonds — commodity-driven inflation risk dominates over flight-to-quality.

**The `tlt_safe_haven_test()` result** classifies TLT's behavior per window as: PASSED, PARTIAL, or FAILED as a safe haven. Gold is included for comparison — both should fail during the shock, both should partially recover as diplomacy reduces inflation expectations in May.

**Cell 20:**
```python
# ── TLT safe haven test ───────────────────────────────────────────────────────
tlt_t = event_study.tlt_safe_haven_test(prices)
print('=== TLT vs Gold — Safe Haven Test Per Window ===\n')
if not tlt_t.empty:
    display(tlt_t[['window_label','asset_label','cum_ret_%','corr_vs_sp500','safe_haven_quality']]
            .applymap(lambda x: fmt_pct(x) if isinstance(x,float) else x))
    callout(
        '<b>Safe Haven Regime Map:</b><br>'
        'Financial crisis (2008/2020): TLT ↑, Gold ↑ — flight to quality<br>'
        'Supply shock (2026): TLT ↓, Gold ↓ — inflation premium + margin calls<br>'
        'Paradox Correction (May): TLT partially recovers — peace reduces inflation expectations',
        title='Supply Shock ≠ Financial Crisis',
        color='#17becf'
    )
else:
    note('TLT not in price data.')
```

**Output:**
```
=== TLT vs Gold — Safe Haven Test Per Window ===
```
```
         window_label         asset_label cum_ret_% corr_vs_sp500  \
0      Hormuz Closure  Treasury ETF (TLT)     -3.3%         +0.4%   
1      Hormuz Closure                Gold     -9.6%         +0.3%   
2          False Dawn  Treasury ETF (TLT)     -1.7%         +0.6%   
3          False Dawn                Gold     -5.0%         +0.9%   
4  Paradox Correction  Treasury ETF (TLT)     +0.2%         +0.9%   
5  Paradox Correction                Gold     -1.5%         +0.8%   
6  Volatile Diplomacy  Treasury ETF (TLT)     +0.3%         +0.7%   
7  Volatile Diplomacy                Gold     -5.8%         +0.7%   

                                  safe_haven_quality  
0  FAILED: negative return and positive equity co...  
1  FAILED: negative return and positive equity co...  
2  FAILED: negative return and positive equity co...  
3  FAILED: negative return and positive equity co...  
4  PARTIAL: positive return but moving with equities  
5  FAILED: negative return and positive equity co...  
6  PARTIAL: positive return but moving with equities  
7  FAILED: negative return and positive equity co...
```
```
<IPython.core.display.HTML object>
```

**Cell 21:**
```python
# ── Chart 24: Safe haven comparison ──────────────────────────────────────────
sh_assets = [a for a in ['TLT','GOLD','SP500'] if a in prices.columns]
if 'TLT' in prices.columns:
    sub_sh = prices.loc['2026-01-01':FETCH_END, sh_assets].dropna(how='all').ffill()
    reb_sh = (sub_sh / sub_sh.iloc[0]) * 100

    fig, ax = plt.subplots(figsize=(15, 7))
    fig.patch.set_facecolor('#0d1117'); ax.set_facecolor('#0d1117')

    add_bands(ax, label_top=True, fontsize=6.8)
    for col in sh_assets:
        lw = 2.6 if col in ('TLT','GOLD') else 1.8
        ax.plot(reb_sh.index, reb_sh[col], label=LABELS.get(col,col),
                color=COLORS.get(col,'#aaa'), lw=lw, alpha=0.90)
    ax.axhline(100, color='#2d333b', lw=0.8, linestyle='--', alpha=0.7)

    # Annotate TLT at shock trough
    if 'TLT' in reb_sh.columns:
        tlt_shock = reb_sh['TLT'].loc['2026-02-28':'2026-04-16'].dropna()
        if not tlt_shock.empty:
            tv, td = tlt_shock.min(), tlt_shock.idxmin()
            ann(ax, f'TLT shock trough: {tv:.0f}\nInflation premium > flight-to-quality',
                xy=(td, tv), xytext=(15,-52), color='#17becf')

    style_dates(ax); ax.set_xlim(reb_sh.index[0], reb_sh.index[-1])
    ax.set_ylabel('Rebased (Jan 1 = 100)', fontsize=9, color='#6e7681')
    ax.set_title('TLT vs Gold — Safe Haven Comparison in a Supply Shock\n'
                 'Supply shocks raise inflation expectations → yields rise → TLT falls\n'
                 'May recovery: peace reduces inflation premium temporarily',
                 fontsize=12, fontweight='700', color='#f0f6fc', linespacing=1.45)
    ax.legend(fontsize=9, labelcolor='#c9d1d9'); ax.grid(alpha=0.2, lw=0.4)
    fig.text(0.01,-0.02,
             'TLT = iShares 20+ Year Treasury Bond ETF · '
             'Supply shocks ≠ financial crises · Long-duration bonds face structural headwinds from inflation premium.',
             fontsize=7, color='#484f58')
    plt.tight_layout()
    save_fig(fig, '24_tlt_safe_haven')
    plt.show()
else:
    note('TLT not in price data.')
```

**Output:**
```
  → c:\Users\User\Desktop\2026-hormuz-blockade-analysis\outputs\update_2\charts\24_tlt_safe_haven.png
```
```
<Figure size 1500x700 with 1 Axes>
```

## Part 10: Aramco — The Exporter Sovereign Perspective

Saudi Aramco is analytically distinct from Western energy equities. Three differences:

1. **Production decision maker is the state**, not a board responding to shareholders
2. **East-West Pipeline bypass** — Aramco rerouted production through the 1.2M bbl/day East-West Pipeline, avoiding the Strait entirely. This is why its EPS beat was large even during a closure it was technically subject to.
3. **Sovereign risk premium** — geopolitical crises near Saudi Arabia raise the discount rate applied to Aramco equity regardless of financial performance

**The sovereign discount hypothesis:** Aramco's equity should trade below its fundamental value (implied by EPS beats and FCF) because investors price in: state dividend decisions, production quota compliance with OPEC+, and geopolitical proximity risk.

**Cell 22:**
```python
# ── Aramco sovereign discount ─────────────────────────────────────────────────
aramco_d = event_study.aramco_sovereign_discount(prices)
print('=== Aramco Sovereign Discount Analysis ===\n')
if not aramco_d.empty:
    display(aramco_d.applymap(lambda x: fmt_pct(x) if isinstance(x,float) else x))
elif isinstance(aramco_d, dict):
    for k,v in aramco_d.items():
        if k not in ('ratio_series',):
            print(f'  {k}: {v}')
else:
    note('ARAMCO not in price data or insufficient data.')
```

**Output:**
```
=== Aramco Sovereign Discount Analysis ===
```
```
       window        window_label aramco_ret_% wti_ret_% discount_%  \
0       shock      Hormuz Closure        +8.0%    +32.9%     -24.9%   
1      reopen          False Dawn        +2.1%    +25.3%     -23.2%   
2  correction  Paradox Correction        +1.4%    -14.3%     +15.8%   
3   diplomacy  Volatile Diplomacy        +0.6%     -7.9%      +8.5%   

                                     discount_interp  n_days_aramco  \
0  Sovereign discount widening: Aramco lagged oil...             30   
1  Sovereign discount widening: Aramco lagged oil...             10   
2  Sovereign premium: Aramco outpaced crude (disc...             20   
3  Sovereign premium: Aramco outpaced crude (disc...              9   

                                              caveat  
0  ARAMCO forward-filled from Tadawul (Sun–Thu, U...  
1  ARAMCO forward-filled from Tadawul (Sun–Thu, U...  
2  ARAMCO forward-filled from Tadawul (Sun–Thu, U...  
3  ARAMCO forward-filled from Tadawul (Sun–Thu, U...
```

**Cell 23:**
```python
# ── Chart 25: Aramco exporter perspective ─────────────────────────────────────
if 'ARAMCO' in prices.columns:
    sub_ar = prices.loc['2026-01-01':FETCH_END, ['ARAMCO','WTI','XOM']].dropna(how='all').ffill()
    reb_ar = (sub_ar / sub_ar.iloc[0]) * 100

    fig, ax = plt.subplots(figsize=(15, 7))
    fig.patch.set_facecolor('#0d1117'); ax.set_facecolor('#0d1117')

    add_bands(ax, label_top=True, fontsize=6.8)
    for col in ['WTI','ARAMCO','XOM']:
        if col not in reb_ar.columns: continue
        lw = 2.8 if col=='WTI' else 2.2
        ax.plot(reb_ar.index, reb_ar[col], label=LABELS.get(col,col),
                color=COLORS.get(col,'#aaa'), lw=lw, alpha=0.90)

    ax.axhline(100, color='#2d333b', lw=0.8, linestyle='--', alpha=0.7)

    # Annotate gap at shock end
    if 'ARAMCO' in reb_ar.columns and 'WTI' in reb_ar.columns:
        se = pd.Timestamp('2026-04-16')
        si = reb_ar.index[reb_ar.index <= se]
        if len(si):
            wv = float(reb_ar.loc[si[-1],'WTI'])
            av = float(reb_ar.loc[si[-1],'ARAMCO'])
            gap = wv - av
            ann(ax, f'Shock end sovereign gap:\nWTI {wv:.0f} vs Aramco {av:.0f}\n(+{gap:.0f}pt discount)',
                xy=(si[-1], av), xytext=(-125, 30), color='#b8860b')

    style_dates(ax); ax.set_xlim(reb_ar.index[0], reb_ar.index[-1])
    ax.set_ylabel('Rebased (Jan 1 = 100)', fontsize=9, color='#6e7681')
    ax.set_title('Saudi Aramco — Exporter Sovereign Paradox\n'
                 'EPS beat +22%, FCF +62% via East-West Pipeline bypass · '
                 'Yet sovereign discount persisted vs WTI',
                 fontsize=12, fontweight='700', color='#f0f6fc', linespacing=1.45)
    ax.legend(fontsize=9, labelcolor='#c9d1d9'); ax.grid(alpha=0.2, lw=0.4)
    fig.text(0.01,-0.02,
             'ARAMCO (2222.SR): Tadawul (Sun–Thu UTC+3) · Reindexed to US calendar via forward-fill · '
             '~18h same-day lag · Directional indicator only.',
             fontsize=7, color='#484f58')
    plt.tight_layout()
    save_fig(fig, '25_aramco_exporter_perspective')
    plt.show()
else:
    note('ARAMCO not in price data.')
```

**Output:**
```
  → c:\Users\User\Desktop\2026-hormuz-blockade-analysis\outputs\update_2\charts\25_aramco_exporter_perspective.png
```
```
<Figure size 1500x700 with 1 Axes>
```

## Part 11: Extended Pair Trades — Long WTI vs Short Energy Equities

Four pair trades tested across all 5 windows:

| Pair | Thesis | New? |
|------|--------|------|
| Long WTI / Short XOM | Original decoupling | Update 1 |
| Long WTI / Short XLE | Same thesis, entire sector | **Update 2** |
| Long ITA / Short SP500 | Defense escalation premium | **Update 2** |
| Long BWET / Short WTI | Freight outpaces commodity | **Update 2** |

**The May Paradox test:** The May correction should have *closed* the Long WTI / Short XOM trade as WTI fell and XOM partially recovered. The pair trade P&L timeseries shows exactly when the dislocation peaked and reversed.

**Cell 24:**
```python
# ── All pair trades ────────────────────────────────────────────────────────────
pair_trades = event_study.pair_trade_extended(prices)
print('=== Extended Pair Trade P&L — All Pairs, All 5 Windows ===\n')
if not pair_trades.empty:
    for pl in pair_trades['pair_label'].unique():
        sub = pair_trades[pair_trades['pair_label']==pl]
        print(f'\n── {pl} ──')
        disp = sub[['window','long_ret_%','short_leg_%','combined_%','n_days']].copy()
        disp['window'] = disp['window'].map(lambda x: WL.get(x,x))
        display(disp.applymap(lambda x: fmt_pct(x) if isinstance(x,float) else x))
    note(pair_trades.iloc[0]['caveat'])
```

**Output:**
```
=== Extended Pair Trade P&L — All Pairs, All 5 Windows ===


── Long WTI / Short XOM ──
```
```
                window long_ret_% short_leg_% combined_%  n_days
0            Pre-event     +16.9%      -25.2%      -8.2%      39
4       Hormuz Closure     +32.9%       +1.4%     +34.4%      33
8           False Dawn     +25.3%       -5.4%     +19.9%      10
12  Paradox Correction     -14.3%       +4.3%     -10.0%      20
16  Volatile Diplomacy      -7.9%       +1.6%      -6.3%      10
```
```

── Long WTI / Short XLE ──
```
```
                window long_ret_% short_leg_% combined_%  n_days
1            Pre-event     +16.9%      -22.5%      -5.6%      39
5       Hormuz Closure     +32.9%       +0.2%     +33.1%      33
9           False Dawn     +25.3%       -8.4%     +16.9%      10
13  Paradox Correction     -14.3%       +4.3%      -9.9%      20
17  Volatile Diplomacy      -7.9%       -0.4%      -8.3%      10
```
```

── Long ITA / Short SP500 ──
```
```
                window long_ret_% short_leg_% combined_%  n_days
2            Pre-event      +9.8%       -0.3%      +9.5%      39
6       Hormuz Closure      -8.5%       -2.3%     -10.9%      33
10          False Dawn      -5.8%       -1.2%      -6.9%      10
14  Paradox Correction      +8.9%       -4.8%      +4.0%      20
18  Volatile Diplomacy      +1.7%       +2.2%      +3.9%      10
```
```

── Long BWET / Short WTI ──
```
```
                window long_ret_% short_leg_% combined_%  n_days
3            Pre-event    +205.9%      -16.9%    +189.0%      39
7       Hormuz Closure     +71.1%      -32.9%     +38.2%      33
11          False Dawn      +9.9%      -25.3%     -15.4%      10
15  Paradox Correction      -8.8%      +14.3%      +5.5%      20
19  Volatile Diplomacy     +20.5%       +7.9%     +28.4%      10
```
```
<IPython.core.display.HTML object>
```

**Cell 25:**
```python
# ── Chart 26 + 29: Pair trade timeseries ──────────────────────────────────────
from event_study import pair_trade_timeseries

valid_pairs = [(la,sa,lb) for la,sa,lb in [
    ('WTI','XOM','Long WTI / Short XOM'),
    ('WTI','XLE','Long WTI / Short XLE'),
] if la in prices.columns and sa in prices.columns]

if valid_pairs:
    fig, axes = plt.subplots(1, len(valid_pairs), figsize=(16, 7))
    if len(valid_pairs)==1: axes = [axes]
    fig.patch.set_facecolor('#0d1117')
    fig.suptitle('Pair Trade P&L — Long WTI vs Short Energy Equities\n'
                 'Pre-cost upper bounds · No borrow, roll, or slippage costs included',
                 fontsize=12, fontweight='700', color='#f0f6fc', y=1.01)

    pair_colors = [('#2ca02c','#d62728'), ('#17becf','#c44e52')]
    for ax, (la,sa,lb), (gc,rc) in zip(axes, valid_pairs, pair_colors):
        ax.set_facecolor('#0d1117')
        ts = pair_trade_timeseries(prices, la, sa)
        if ts.empty: continue
        add_bands(ax, label_top=False)
        ax.plot(ts.index, ts['long_cum'],  label=f'Long {LABELS.get(la,la)}',
                color=rc, lw=2.0, alpha=0.80)
        ax.plot(ts.index, ts['short_cum'], label=f'Short {LABELS.get(sa,sa)}',
                color=rc, lw=1.6, alpha=0.65, linestyle='--')
        ax.plot(ts.index, ts['pair_cum'],  label='Combined L/S',
                color=gc, lw=2.8)
        ax.axhline(100, color='#2d333b', lw=0.7, linestyle='--')
        style_dates(ax)
        ax.set_ylabel('Cumulative P&L (Base = 100)', fontsize=9, color='#6e7681')
        ax.set_title(lb, fontsize=11, fontweight='700', color='#f0f6fc')
        ax.legend(fontsize=8.5, labelcolor='#c9d1d9'); ax.grid(alpha=0.2, lw=0.4)
    plt.tight_layout()
    save_fig(fig, '29_all_pair_trades_pnl')
    plt.show()
```

**Output:**
```
  → c:\Users\User\Desktop\2026-hormuz-blockade-analysis\outputs\update_2\charts\29_all_pair_trades_pnl.png
```
```
<Figure size 1600x700 with 2 Axes>
```

## Part 12: Cross-Asset Correlation — 5-Window Regime Shifts

The correlation analysis shows whether the regime shifts identified by the WTI/Gold ratio are confirmed by cross-asset correlation patterns.

**What the May Correction should show:**
- WTI/SP500: correlation normalizes toward zero (oil correcting ≈ equities recovering)
- DXY/WTI: flips back toward historical negative (supply signal fades)
- BWET/WTI: stays high (freight and oil still both subject to Hormuz risk)
- TLT/SP500: should improve (less inflation risk as oil falls = bond-equity relationship partially normalizes)

The `cross_asset_correlation_delta()` function shows the delta from pre-event baseline — how much each relationship *changed* and whether it reverted.

**Cell 26:**
```python
# ── Correlation analysis ──────────────────────────────────────────────────────
corr_win   = event_study.correlation_by_window(prices)
corr_flip_ = event_study.correlation_flip(prices)
corr_delta = event_study.cross_asset_correlation_delta(prices)

print('=== WTI/SP500 Correlation — 5-Window Evolution ===\n')
for k in ['pre_avg','shock_avg','reopen_avg','correction_avg','diplomacy_avg','flip_delta']:
    v = corr_flip_.get(k)
    if v is not None:
        print(f'  {k:<22}: {v:>+.3f}')
print(f'\n  Regime: {corr_flip_.get("interpretation")}')

print('\n=== Correlation Delta From Pre-Event Baseline ===')
if not corr_delta.empty:
    delta_cols = [c for c in corr_delta.columns if c.startswith('delta_')]
    display(corr_delta[delta_cols].applymap(
        lambda x: f'{x:+.3f}' if pd.notna(x) else '—'))
```

**Output:**
```
=== WTI/SP500 Correlation — 5-Window Evolution ===

  pre_avg               : +0.002
  shock_avg             : -0.577
  reopen_avg            : -0.714
  correction_avg        : -0.647
  diplomacy_avg         : -0.088
  flip_delta            : -0.579

  Regime: Inverse shift: oil and equities decoupled during closure.

=== Correlation Delta From Pre-Event Baseline ===
```
```
             delta_shock delta_reopen delta_correction delta_diplomacy
pair                                                                  
BWET / WTI        -0.157       -0.765           -0.032          -0.002
DXY / WTI         +0.844       +0.683           +0.688          +0.558
GOLD / SP500      +0.216       +0.813           +0.651          +0.570
ITA / SP500       +0.314       -0.061           +0.331          +0.132
TLT / SP500       +0.501       +0.664           +0.923          +0.719
TLT / WTI         +0.053       -0.522           -0.496          -0.053
WTI / SP500       -0.579       -0.716           -0.649          -0.090
XLE / WTI         -0.191       +0.096           +0.107          +0.076
```

**Cell 27:**
```python
# ── Chart 04-U2: Rolling correlation full timeline ────────────────────────────
roll_c = event_study.oil_equity_correlation(prices, start='2026-01-01', end=FETCH_END)

fig, ax = plt.subplots(figsize=(15, 5.5))
fig.patch.set_facecolor('#0d1117'); ax.set_facecolor('#0d1117')
add_bands(ax, label_top=True, fontsize=6.8)

if not roll_c.empty:
    cn = roll_c.columns[0]
    ax.fill_between(roll_c.index, 0, roll_c[cn],
                    where=roll_c[cn]<0, color='#d62728', alpha=0.10)
    ax.fill_between(roll_c.index, 0, roll_c[cn],
                    where=roll_c[cn]>=0, color='#2ca02c', alpha=0.08)
    ax.plot(roll_c.index, roll_c[cn], color='#d62728', lw=2.2,
            label='WTI / SP500 (10d rolling)')
    ax.axhline(corr_flip_.get('shock_avg',0), color='#d62728',
               lw=0.8, linestyle=':', alpha=0.6)
    shock_v = corr_flip_.get('shock_avg', 0) or 0
    ax.text(pd.Timestamp('2026-03-20'), shock_v-0.06,
            f'Shock avg: {shock_v:+.2f}', fontsize=7.5,
            color='#d62728', style='italic')

ax.axhline(0, color='#333', lw=0.8, linestyle='--')
ax.set_ylim(-1.05, 1.05)
style_dates(ax); ax.set_xlim(roll_c.index[0], roll_c.index[-1])
ax.set_ylabel('Rolling 10-Day Pearson Correlation', fontsize=9, color='#6e7681')
ax.set_title('WTI / S&P 500 Rolling Correlation — 5-Window Regime Evolution\n'
             'Correction window: correlation should normalise (oil fell with equities recovering)',
             fontsize=12, fontweight='700', color='#f0f6fc')
ax.legend(fontsize=9, labelcolor='#c9d1d9'); ax.grid(alpha=0.2, lw=0.4)
plt.tight_layout()
save_fig(fig, '04_rolling_correlation_u2')
plt.show()
```

**Output:**
```
  → c:\Users\User\Desktop\2026-hormuz-blockade-analysis\outputs\update_2\charts\04_rolling_correlation_u2.png
```
```
<Figure size 1500x550 with 1 Axes>
```

## Part 13: Regime Summary — Five Phases Classified

Every analytical thread in this study resolves into a single per-window regime label. The table synthesises the WTI directional move, WTI/Gold ratio direction, and WTI/SP500 correlation into one of four regimes: `SUPPLY_SHOCK`, `DIPLOMACY_BREAK`, `RESOLUTION`, or `PRE_EVENT`.

The May Paradox should classify as `DIPLOMACY_BREAK` — oil fell, WTI/Gold ratio fell, strait still closed.

**Cell 28:**
```python
# ── Regime summary ────────────────────────────────────────────────────────────
regime_s = event_study.window_regime_summary(prices, og_ratio, corr_flip_)
print('=== Window Regime Classification ===\n')
if not regime_s.empty:
    display(regime_s.applymap(lambda x: fmt_pct(x) if isinstance(x,float) else x))

    # Summary KPI
    regimes = dict(zip(regime_s['window_label'], regime_s['regime']))
    section_kpi([
        (lbl, regime, '#ff7f0e' if 'DIPLOMACY' in regime else
                      '#d62728' if 'SUPPLY' in regime else '#6e7681')
        for lbl, regime in regimes.items()
    ])
```

**Output:**
```
=== Window Regime Classification ===
```
```
       window        window_label wti_ret_% og_ratio_chg_% wti_sp500_corr  \
0   pre_event           Pre-event    +16.9%          -3.6%            N/A   
1       shock      Hormuz Closure    +32.9%         +47.1%          -0.6%   
2      reopen          False Dawn    +25.3%         +31.9%          -0.7%   
3  correction  Paradox Correction    -14.3%         -13.0%          -0.6%   
4   diplomacy  Volatile Diplomacy     -7.9%          -2.2%          -0.1%   

            regime  
0        PRE_EVENT  
1     SUPPLY_SHOCK  
2     SUPPLY_SHOCK  
3  DIPLOMACY_BREAK  
4       RESOLUTION
```
```
<IPython.core.display.HTML object>
```

## Part 14: Rolling Beta — Original and New Assets

Beta evolution through the five windows tests the backlog thesis quantitatively. ITA's beta should stay above 1 (elevated market sensitivity) even as WTI corrects in May — because defense earnings aren't affected by oil prices and ITA was priced as a high-growth war-spending story.

**Cell 29:**
```python
# ── Rolling betas ─────────────────────────────────────────────────────────────
beta_assets = [a for a in ['ITA','BWET','TLT','XLE','XOM','GOLD','JETS'] if a in prices.columns]
rb = event_study.rolling_beta_series(prices, assets=beta_assets,
                                     start='2026-01-01', end=FETCH_END)

if not rb.empty:
    print('=== Rolling Beta Per-Window Averages ===\n')
    for wname, (ws, we) in EW.items():
        sub_b = rb.loc[ws:we].dropna()
        if sub_b.empty: continue
        means = sub_b.mean().round(3).to_dict()
        print(f'{WL.get(wname,wname)}: {means}')

    fig, (ax1, ax2) = plt.subplots(2, 1, figsize=(15, 9))
    fig.patch.set_facecolor('#0d1117')
    for ax in [ax1,ax2]: ax.set_facecolor('#0d1117')

    new_betas  = [c for c in ['ITA','TLT','BWET','XLE'] if c in rb.columns]
    orig_betas = [c for c in ['XOM','GOLD','JETS'] if c in rb.columns]

    for ax, cols, ttl in [
        (ax1, new_betas,  'Update 2 New Assets — Rolling 21-Day Beta vs SP500'),
        (ax2, orig_betas, 'Original Assets — Rolling Beta (for comparison)'),
    ]:
        add_bands(ax, label_top=(ax is ax1), fontsize=6.8)
        for col in cols:
            ax.plot(rb.index, rb[col], label=LABELS.get(col,col),
                    color=COLORS.get(col,'#aaa'), lw=2.0, alpha=0.88)
        ax.axhline(1.0, color='#444', lw=0.8, linestyle='--', alpha=0.7, label='beta=1')
        ax.axhline(0.0, color='#2d333b', lw=0.5, linestyle=':')
        style_dates(ax); ax.set_xlim(rb.index[0], rb.index[-1])
        ax.set_ylabel('Rolling Beta vs SP500', fontsize=9, color='#6e7681')
        ax.set_title(ttl, fontsize=10.5, fontweight='700', color='#f0f6fc')
        ax.legend(fontsize=8.5, labelcolor='#c9d1d9', ncol=2); ax.grid(alpha=0.2, lw=0.4)

    plt.tight_layout()
    save_fig(fig, '13_rolling_beta_u2')
    plt.show()
```

**Output:**
```
=== Rolling Beta Per-Window Averages ===

Pre-event: {'ITA': 0.802, 'BWET': 3.0, 'TLT': -0.003, 'XLE': 0.244, 'XOM': 0.163, 'GOLD': 0.356, 'JETS': 1.481}
Hormuz Closure: {'ITA': 1.001, 'BWET': 2.196, 'TLT': 0.17, 'XLE': -0.212, 'XOM': -0.472, 'GOLD': 0.458, 'JETS': 1.794}
False Dawn: {'ITA': 1.471, 'BWET': -0.775, 'TLT': 0.197, 'XLE': -1.135, 'XOM': -1.39, 'GOLD': 1.04, 'JETS': 2.155}
Paradox Correction: {'ITA': 1.496, 'BWET': -0.651, 'TLT': 0.62, 'XLE': -1.286, 'XOM': -1.672, 'GOLD': 1.56, 'JETS': 2.206}
Volatile Diplomacy: {'ITA': 1.36, 'BWET': 0.121, 'TLT': 0.592, 'XLE': -0.706, 'XOM': -1.268, 'GOLD': 1.113, 'JETS': 2.049}
  → c:\Users\User\Desktop\2026-hormuz-blockade-analysis\outputs\update_2\charts\13_rolling_beta_u2.png
```
```
<Figure size 1500x900 with 2 Axes>
```

## Part 15: Abnormal Returns — All 5 Windows

The OLS market model (pre-event beta) decomposes each window's return into: expected (from market beta) and abnormal (shock-specific).

**Correction window is the key comparison:** In the shock window, WTI's +20.9% abnormal return quantified the pure Hormuz effect. In the correction window, WTI's abnormal return should be *negative* — quantifying how much of the drop was genuine expectations-driven (not explainable by market movement). If ITA shows positive abnormal return persisting through correction, that is direct evidence of the backlog thesis.

**Cell 30:**
```python
# ── Abnormal returns ──────────────────────────────────────────────────────────
ab_assets = [a for a in ['WTI','GOLD','XOM','XLE','ITA','BWET','TLT','JETS'] if a in prices.columns]
abnormal = event_study.compute_abnormal_returns(prices, ab_assets, benchmark='SP500')

print('=== OLS Abnormal Returns — All Post-Event Windows ===\n')
for wname in ['shock','reopen','correction','diplomacy']:
    sub = abnormal[abnormal['window']==wname].copy()
    if sub.empty: continue
    sub = sub.sort_values('abnormal_ret_%', ascending=False)
    sub['asset'] = sub['asset'].map(lambda x: LABELS.get(x,x))
    print(f'\n── {WL.get(wname,wname)} ──')
    display(sub[['asset','actual_ret_%','expected_ret_%','abnormal_ret_%','beta','r2']]
            .applymap(lambda x: fmt_pct(x) if isinstance(x,float) else x))
```

**Output:**
```
=== OLS Abnormal Returns — All Post-Event Windows ===


── Hormuz Closure ──
```
```
                  asset actual_ret_% expected_ret_% abnormal_ret_%   beta  \
0             WTI Crude       +32.9%         +10.4%         +22.5%  -0.0%   
24   Treasury ETF (TLT)        -3.3%          +2.9%          -6.2%  -0.1%   
28         Airlines ETF        -3.9%          +3.1%          -6.9%  +1.6%   
12  Energy Sector (XLE)        -0.2%         +13.1%         -13.3%  +0.1%   
16    Defense ETF (ITA)        -8.5%          +7.4%         -15.9%  +0.8%   
8           Exxon (XOM)        -1.4%         +14.9%         -16.3%  -0.0%   
20    Tanker ETF (BWET)       +71.1%         +92.4%         -21.3%  +3.0%   
4                  Gold        -9.6%         +13.4%         -23.1%  +0.3%   

       r2  
0    0.0%  
24  +0.0%  
28  +0.3%  
12  +0.0%  
16  +0.2%  
8    0.0%  
20  +0.1%  
4   +0.0%
```
```

── False Dawn ──
```
```
                  asset actual_ret_% expected_ret_% abnormal_ret_%   beta  \
1             WTI Crude       +25.3%          +2.9%         +22.4%  -0.0%   
13  Energy Sector (XLE)        +8.4%          +3.8%          +4.7%  +0.1%   
9           Exxon (XOM)        +5.4%          +4.2%          +1.2%  -0.0%   
25   Treasury ETF (TLT)        -1.7%          +0.8%          -2.4%  -0.1%   
17    Defense ETF (ITA)        -5.8%          +2.5%          -8.2%  +0.8%   
5                  Gold        -5.0%          +3.9%          -8.9%  +0.3%   
29         Airlines ETF        -8.9%          +1.7%         -10.6%  +1.6%   
21    Tanker ETF (BWET)        +9.9%         +27.5%         -17.6%  +3.0%   

       r2  
1    0.0%  
13  +0.0%  
9    0.0%  
25  +0.0%  
17  +0.2%  
5   +0.0%  
29  +0.3%  
21  +0.1%
```
```

── Paradox Correction ──
```
```
                  asset actual_ret_% expected_ret_% abnormal_ret_%   beta  \
30         Airlines ETF       +13.8%          +7.2%          +6.6%  +1.6%   
18    Defense ETF (ITA)        +8.9%          +7.1%          +1.8%  +0.8%   
26   Treasury ETF (TLT)        +0.2%          +1.5%          -1.3%  -0.1%   
6                  Gold        -1.5%          +9.0%         -10.5%  +0.3%   
14  Energy Sector (XLE)        -4.3%          +8.2%         -12.6%  +0.1%   
10          Exxon (XOM)        -4.3%          +8.7%         -13.0%  -0.0%   
2             WTI Crude       -14.3%          +6.1%         -20.4%  -0.0%   
22    Tanker ETF (BWET)        -8.8%         +65.3%         -74.1%  +3.0%   

       r2  
30  +0.3%  
18  +0.2%  
26  +0.0%  
6   +0.0%  
14  +0.0%  
10   0.0%  
2    0.0%  
22  +0.1%
```
```

── Volatile Diplomacy ──
```
```
                  asset actual_ret_% expected_ret_% abnormal_ret_%   beta  \
31         Airlines ETF        +2.1%          -3.6%          +5.7%  +1.6%   
23    Tanker ETF (BWET)       +20.5%         +17.3%          +3.2%  +3.0%   
19    Defense ETF (ITA)        +1.7%          -0.2%          +1.9%  +0.8%   
27   Treasury ETF (TLT)        +0.3%          +1.0%          -0.6%  -0.1%   
15  Energy Sector (XLE)        +0.4%          +3.3%          -2.9%  +0.1%   
11          Exxon (XOM)        -1.6%          +4.2%          -5.8%  -0.0%   
7                  Gold        -5.8%          +2.9%          -8.8%  +0.3%   
3             WTI Crude        -7.9%          +3.0%         -10.9%  -0.0%   

       r2  
31  +0.3%  
23  +0.1%  
19  +0.2%  
27  +0.0%  
15  +0.0%  
11   0.0%  
7   +0.0%  
3    0.0%
```

**Cell 31:**
```python
# ── Chart 05-U2: Shock vs Correction abnormal returns ─────────────────────────
fig, axes = plt.subplots(1, 2, figsize=(16, 7))
fig.patch.set_facecolor('#0d1117')
fig.suptitle('OLS Abnormal Returns: Shock vs Paradox Correction\n'
             'Shock: WTI anomalously high · Correction: positive anomaly reverses',
             fontsize=12, fontweight='700', color='#f0f6fc', y=1.01)

for ax, wname in zip(axes, ['shock','correction']):
    ax.set_facecolor('#0d1117')
    sub = abnormal[abnormal['window']==wname].sort_values('abnormal_ret_%')
    if sub.empty:
        ax.text(0.5,0.5,'No data',ha='center',va='center',transform=ax.transAxes,color='#6e7681')
        continue
    y = [LABELS.get(a,a) for a in sub['asset']]
    x = sub['abnormal_ret_%'].values
    bc = ['#d62728' if v>0 else '#17becf' for v in x]
    bars = ax.barh(y, x, color=bc, alpha=0.85, edgecolor='#0d1117', height=0.65)
    for bar, v in zip(bars, x):
        ax.text(v+(0.3 if v>=0 else -0.3), bar.get_y()+bar.get_height()/2,
                fmt_pct(v), va='center', ha='left' if v>=0 else 'right',
                fontsize=8.5, fontweight='bold', color='#f0f6fc')
    ax.axvline(0, color='#333', lw=0.8)
    ax.set_xlabel('Abnormal Return %', fontsize=9, color='#8b949e')
    ax.xaxis.set_major_formatter(mtick.PercentFormatter(decimals=0))
    ax.set_title(f'{WL.get(wname,wname)}\nAbnormal returns (OLS market model)',
                 fontsize=10.5, fontweight='700', color='#f0f6fc')
    ax.grid(axis='x', alpha=0.2, lw=0.4)
    ax.tick_params(axis='y', labelsize=9, colors='#c9d1d9')

plt.tight_layout()
save_fig(fig, '05_abnormal_returns_u2')
plt.show()
```

**Output:**
```
  → c:\Users\User\Desktop\2026-hormuz-blockade-analysis\outputs\update_2\charts\05_abnormal_returns_u2.png
```
```
<Figure size 1600x700 with 2 Axes>
```

## Part 16: Lead-Lag CCF — Updated

Updated through June 13. Key question for Update 2: does the WTI/SP500 CCF shift during the correction window? If WTI started leading SP500 *negatively* even as it fell (i.e., WTI's fall preceded equity recovery), that would suggest oil was acting as a *positive* leading indicator for equities in May — the inversion of the shock-window relationship.

**Cell 32:**
```python
# ── Lead-lag CCF ──────────────────────────────────────────────────────────────
ccf_full = event_study.lead_lag_ccf(prices, leader='WTI', follower='SP500',
                                     start='2026-01-01', end=FETCH_END)

print('=== Lead-Lag CCF Peak Per Window ===\n')
for wname, (ws, we) in EW.items():
    ccf_w = event_study.lead_lag_ccf(prices, leader='WTI', follower='SP500',
                                      start=ws, end=we)
    if ccf_w.empty: continue
    peak = ccf_w[ccf_w['is_peak']]
    if not peak.empty:
        lv = int(peak['lag'].iloc[0]); cv = peak['correlation'].iloc[0]
        interp = 'simultaneous' if lv==0 else f'WTI leads by {lv}d' if lv>0 else f'SP500 leads by {abs(lv)}d'
        print(f'  {WL.get(wname,wname):<25}: lag {lv:+d} (r={cv:+.3f}) → {interp}')

fig, ax = plt.subplots(figsize=(11, 5.5))
fig.patch.set_facecolor('#0d1117'); ax.set_facecolor('#0d1117')
if not ccf_full.empty:
    bc = ['#d62728' if r['is_peak'] else '#21262d' for _,r in ccf_full.iterrows()]
    bars = ax.bar(ccf_full['lag'], ccf_full['correlation'],
                  color=bc, alpha=0.85, edgecolor='#0d1117', width=0.7)
    for bar, v in zip(bars, ccf_full['correlation']):
        ax.text(bar.get_x()+bar.get_width()/2, v+(0.01 if v>=0 else -0.02),
                f'{v:.3f}', ha='center', va='bottom' if v>=0 else 'top',
                fontsize=7.5, color='#c9d1d9')
ax.axhline(0, color='#444', lw=0.8)
ax.set_xlabel('Lag (days · positive = WTI leads)', fontsize=9, color='#8b949e')
ax.set_ylabel('Pearson Correlation', fontsize=9, color='#8b949e')
ax.set_title('Lead-Lag CCF: WTI vs S&P 500 (Jan–Jun 2026)\n'
             'Peak at lag 0 = simultaneous repricing — no exploitable daily lead-lag at this frequency',
             fontsize=12, fontweight='700', color='#f0f6fc')
ax.set_xticks(ccf_full['lag']); ax.grid(axis='y', alpha=0.2, lw=0.4)
plt.tight_layout()
save_fig(fig, '12_lead_lag_ccf_u2')
plt.show()
```

**Output:**
```
=== Lead-Lag CCF Peak Per Window ===

  Pre-event                : lag +2 (r=+0.312) → WTI leads by 2d
  Hormuz Closure           : lag +0 (r=-0.587) → simultaneous
  False Dawn               : lag -5 (r=+0.712) → SP500 leads by 5d
  Paradox Correction       : lag +0 (r=-0.646) → simultaneous
  Volatile Diplomacy       : lag +5 (r=-0.899) → WTI leads by 5d
  → c:\Users\User\Desktop\2026-hormuz-blockade-analysis\outputs\update_2\charts\12_lead_lag_ccf_u2.png
```
```
<Figure size 1100x550 with 1 Axes>
```

## Part 17: DXY Decomposition + International Spillover

**DXY Updated:** The May correction raises a new question — did DXY appreciation explain any of WTI's May decline? If dollar strengthened materially in May (risk-off, safe-haven demand for USD), that would mechanically reduce WTI prices. The decomposition tests this.

**International Spillover Updated:** The correction window may have provided partial relief to oil-importing economies (Japan, Germany). If Nikkei and DAX recovered in May while WTI fell, it confirms the May Paradox — peace pricing benefited equity importers even before the strait actually reopened.

**Cell 33:**
```python
# ── DXY decomposition ─────────────────────────────────────────────────────────
dxy_d = event_study.dxy_oil_decomposition(prices)
print('=== DXY Decomposition — Currency vs Real Supply (Shock Window) ===\n')
if dxy_d.get('available'):
    for k in ['wti_shock_ret_%','dxy_shock_ret_%','beta_oil_on_dxy',
               'currency_contrib_%','real_supply_%','currency_share_%']:
        print(f'  {k:<28}: {dxy_d.get(k)}')
    print(f'  Interpretation: {dxy_d.get("interpretation")}')
    note(dxy_d.get('caveat',''))

# DXY/WTI correlation per window
print('\n=== DXY / WTI Correlation Per Window ===')
if 'DXY' in prices.columns and 'WTI' in prices.columns:
    for wname, (ws, we) in EW.items():
        sub = event_study.simple_returns(prices.loc[ws:we, ['DXY','WTI']]).dropna()
        if len(sub) > 3:
            c = round(sub.corr().iloc[0,1], 3)
            direction = '→ Supply shock signal' if c>0 else '→ Normal inverse'
            print(f'  {WL.get(wname,wname):<25}: {c:+.3f}  {direction}')
```

**Output:**
```
=== DXY Decomposition — Currency vs Real Supply (Shock Window) ===

  wti_shock_ret_%             : 32.94
  dxy_shock_ret_%             : -0.16
  beta_oil_on_dxy             : -1.418
  currency_contrib_%          : 0.23
  real_supply_%               : 32.7
  currency_share_%            : 0.7
  Interpretation: DXY weakened 0.2% during shock. Currency contribution: ~0.2pp (0.7% of total). Real supply contribution: ~32.7pp of the 32.9% WTI move.
```
```
<IPython.core.display.HTML object>
```
```

=== DXY / WTI Correlation Per Window ===
  Pre-event                : -0.244  → Normal inverse
  Hormuz Closure           : +0.587  → Supply shock signal
  False Dawn               : +0.599  → Supply shock signal
  Paradox Correction       : +0.462  → Supply shock signal
  Volatile Diplomacy       : +0.173  → Supply shock signal
```

**Cell 34:**
```python
# ── International spillover ────────────────────────────────────────────────────
intl_s = event_study.international_spillover(prices)
print('=== International Equity Spillover — All 5 Windows ===\n')
if not intl_s.empty:
    piv_r = intl_s.pivot(index='index', columns='window', values='cum_return_%')
    piv_r = piv_r[[c for c in PHASE_ORDER if c in piv_r.columns]]
    piv_r.columns = [WL.get(c,c) for c in piv_r.columns]
    piv_r.index = [LABELS.get(i,i) for i in piv_r.index]
    print('Cumulative Returns:')
    display(piv_r.applymap(lambda x: fmt_pct(x) if pd.notna(x) else '—'))

    piv_d = intl_s.pivot(index='index', columns='window', values='max_dd_%')
    piv_d = piv_d[[c for c in PHASE_ORDER if c in piv_d.columns]]
    piv_d.columns = [WL.get(c,c) for c in piv_d.columns]
    piv_d.index = [LABELS.get(i,i) for i in piv_d.index]
    print('\nMax Drawdowns:')
    display(piv_d.applymap(lambda x: fmt_pct(x) if pd.notna(x) else '—'))

    note('All local-currency returns. No FX adjustment applied.')
```

**Output:**
```
=== International Equity Spillover — All 5 Windows ===

Cumulative Returns:
```
```
              Hormuz Closure False Dawn Paradox Correction Volatile Diplomacy
DAX (Germany)          -2.0%      -1.7%              +4.6%              -1.5%
Nikkei 225             +2.5%      +1.4%             +11.4%              -1.4%
S&P 500                +2.3%      +1.2%              +4.8%              -2.2%
```
```

Max Drawdowns:
```
```
              Hormuz Closure False Dawn Paradox Correction Volatile Diplomacy
DAX (Germany)          -9.5%      -3.0%              -3.9%              -3.7%
Nikkei 225            -12.1%      -2.1%              -5.5%              -6.4%
S&P 500                -7.8%      -0.9%              -2.0%              -4.5%
```
```
<IPython.core.display.HTML object>
```

## Part 18: VIX & Volatility Regimes — Updated

VIX peaked at 31 during the shock — moderate, not crisis-level. The May correction should show VIX declining (peace signal = lower uncertainty). June 10 re-escalation should show a secondary VIX spike smaller than the Feb 28 original if market learning reduced the uncertainty priced in.

**Cell 35:**
```python
# ── VIX peak stats ────────────────────────────────────────────────────────────
vix_stats = event_study.vix_peak_stats(prices, end=FETCH_END)
print('=== VIX Peak Stats (Full Jan–Jun Period) ===\n')
for k, v in vix_stats.items():
    print(f'  {k:<18}: {v}')

# Vol regime periods
vol_assets = [a for a in ['WTI','SP500','GOLD','XOM','ITA','TLT'] if a in prices.columns]
vol_regimes = event_study.vol_regime_periods(prices, assets=vol_assets, end=FETCH_END)

print('\n=== Volatility Regime Detection (5d/21d > 2.0x threshold) ===')
for asset, data in vol_regimes.items():
    print(f'  {LABELS.get(asset,asset):<20}: '
          f'peak ratio={data.get("peak_ratio")}, '
          f'peak date={data.get("peak_ratio_date")}, '
          f'days in regime={data.get("days_in_regime")}')
```

**Output:**
```
=== VIX Peak Stats (Full Jan–Jun Period) ===

  peak_vix          : 31.0
  peak_date         : 2026-03-27
  avg_5yr_vix       : 20.7
  vix_multiple      : 1.5

=== Volatility Regime Detection (5d/21d > 2.0x threshold) ===
  WTI Crude           : peak ratio=1.97, peak date=2026-03-10, days in regime=0
  S&P 500             : peak ratio=1.77, peak date=2026-06-11, days in regime=0
  Gold                : peak ratio=2.05, peak date=2026-02-03, days in regime=1
  Exxon (XOM)         : peak ratio=1.75, peak date=2026-04-01, days in regime=0
  Defense ETF (ITA)   : peak ratio=1.65, peak date=2026-04-01, days in regime=0
  Treasury ETF (TLT)  : peak ratio=1.59, peak date=2026-05-21, days in regime=0
```

## Part 19: Asset Character Cards — All 16 Assets

Full arc per asset through all 5 windows. New assets get their first character card. The `net_jan_jun_%` field is the total return from Jan 1 through the cutoff — the headline number for each asset's Update 2 story.

**Cell 36:**
```python
# ── Character cards ───────────────────────────────────────────────────────────
ev_stats_all = event_study.event_window_stats(prices, all_av)
cards = event_study.build_all_character_cards(prices, seasonal, ev_stats=ev_stats_all)

for asset in all_av:
    card = cards.get(asset, {})
    if not card: continue
    label  = LABELS.get(asset, asset)
    is_new = asset in NEW_ASSETS_U2
    color  = '#ff7f0e' if is_new else '#6e7681'
    tag    = ' ★ NEW' if is_new else ''

    print(f'\n{"━"*55}')
    print(f'  {label}{tag}')
    print(f'{"━"*55}')

    row_keys = ['pre_event_ret','shock_ret','reopen_ret','correction_ret','diplomacy_ret',
                'net_jan_jun_%','peak_gain_%','trough_loss_%','peak_date']
    for k in row_keys:
        v = card.get(k)
        if v is None: continue
        dv = fmt_pct(v) if isinstance(v,(int,float)) else str(v)
        print(f'  {k:<25}: {dv}')

    if card.get('limited_history'):
        print(f'  ⚠  {card.get("limited_history_note")}')
    if asset == 'GOLD' and card.get('margin_call_flag') is not None:
        print(f'  Margin-call flag  : {card["margin_call_flag"]}')
```

**Output:**
```

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  S&P 500
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  pre_event_ret            : +0.3%
  shock_ret                : +2.3%
  reopen_ret               : +1.2%
  correction_ret           : +4.8%
  diplomacy_ret            : -2.2%
  net_jan_jun_%            : +8.4%
  peak_gain_%              : +11.0%
  trough_loss_%            : -7.5%
  peak_date                : 2026-06-02

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  WTI Crude
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  pre_event_ret            : +16.9%
  shock_ret                : +32.9%
  reopen_ret               : +25.3%
  correction_ret           : -14.3%
  diplomacy_ret            : -7.9%
  net_jan_jun_%            : +48.1%
  peak_gain_%              : +97.1%
  trough_loss_%            : -2.3%
... [truncated] ...
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  Saudi Aramco ★ NEW
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  pre_event_ret            : +4.5%
  shock_ret                : +8.0%
  reopen_ret               : +2.1%
  correction_ret           : +1.5%
  diplomacy_ret            : +0.6%
  net_jan_jun_%            : +16.7%
  peak_gain_%              : +18.6%
  trough_loss_%            : -2.0%
  peak_date                : 2026-05-19

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  Natural Gas (UNG) ★ NEW
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  pre_event_ret            : -4.5%
  shock_ret                : -10.2%
  reopen_ret               : -2.2%
  correction_ret           : +11.4%
  diplomacy_ret            : -1.6%
  net_jan_jun_%            : -5.9%
  peak_gain_%              : +40.1%
  trough_loss_%            : -15.8%
  peak_date                : 2026-01-30
```

## Part 20: Key Numbers Summary — Update 2

The complete quantitative reference table. This is the table that gets cited, linked from the dashboard, and included in the README update.

**Cell 37:**
```python
# ── Summary key numbers ────────────────────────────────────────────────────────
def _ev(df, asset, window, col):
    r = df[(df['asset']==asset) & (df['window']==window)]
    return float(r[col].values[0]) if not r.empty else None

focus = ['WTI','SP500','GOLD','XOM','CVX','ITA','BWET','TLT','XLE','JETS']
focus = [a for a in focus if a in prices.columns]

summary_rows = []
for asset in focus:
    summary_rows.append({
        'Asset':            LABELS.get(asset,asset),
        'Shock %':          fmt_pct(_ev(ev_stats_all, asset, 'shock',      'cum_return')),
        'Correction %':     fmt_pct(_ev(ev_stats_all, asset, 'correction', 'cum_return')),
        'Diplomacy %':      fmt_pct(_ev(ev_stats_all, asset, 'diplomacy',  'cum_return')),
        'Shock Max DD %':   fmt_pct(_ev(ev_stats_all, asset, 'shock',      'max_dd')),
        'Shock Vol %':      fmt_pct(_ev(ev_stats_all, asset, 'shock',      'ann_vol')),
        'Day-1 Feb 28 %':  fmt_pct(first_day[(first_day['event']=='Op. Epic Fury (Feb 28)')&
                                              (first_day['asset']==asset)]['day1_return_%'].values[0]
                                   if not first_day[(first_day['event']=='Op. Epic Fury (Feb 28)')&
                                                    (first_day['asset']==asset)].empty else None),
        'Day-1 Jun 10 %':  fmt_pct(first_day[(first_day['event']=='Iran Re-escalation (Jun 10)')&
                                              (first_day['asset']==asset)]['day1_return_%'].values[0]
                                   if not first_day[(first_day['event']=='Iran Re-escalation (Jun 10)')&
                                                    (first_day['asset']==asset)].empty else None),
    })

summary_df = pd.DataFrame(summary_rows)
print('=== Final Key Numbers — Update 2 ===\n')
display(summary_df)

section_kpi([
    ('May Paradox', 'WTI −20%, strait closed', '#ff7f0e'),
    ('Third decoupling', 'BWET >> WTI cumulative', '#ff7f0e'),
    ('Defense thesis', 'ITA held premium in May', '#1f77b4'),
    ('Cutoff', FETCH_END, '#6e7681'),
])
```

**Output:**
```
=== Final Key Numbers — Update 2 ===
```
```
                 Asset Shock % Correction % Diplomacy % Shock Max DD %  \
0            WTI Crude  +32.9%       -14.3%       -7.9%         -19.2%   
1              S&P 500   +2.3%        +4.8%       -2.2%          -7.8%   
2                 Gold   -9.6%        -1.5%       -5.8%         -17.4%   
3          Exxon (XOM)   -1.4%        -4.3%       -1.6%         -13.1%   
4        Chevron (CVX)   -0.8%        -3.4%       +0.8%         -12.4%   
5    Defense ETF (ITA)   -8.5%        +8.9%       +1.7%         -15.8%   
6    Tanker ETF (BWET)  +71.1%        -8.8%      +20.5%         -30.6%   
7   Treasury ETF (TLT)   -3.3%        +0.2%       +0.3%          -4.4%   
8  Energy Sector (XLE)   -0.2%        -4.3%       +0.4%         -10.9%   
9         Airlines ETF   -3.9%       +13.8%       +2.1%         -14.7%   

  Shock Vol % Day-1 Feb 28 % Day-1 Jun 10 %  
0      +91.8%           0.0%          +2.1%  
1      +16.3%           0.0%          -1.6%  
2      +31.7%           0.0%          -3.6%  
3      +27.2%           0.0%          +1.1%  
4      +25.8%            N/A            N/A  
5      +26.0%           0.0%          -2.4%  
6     +163.1%            N/A            N/A  
7      +10.1%           0.0%          -0.3%  
8      +20.8%            N/A            N/A  
9      +35.5%           0.0%          -4.6%
```
```
<IPython.core.display.HTML object>
```

**Cell 38:**
```python
# ── Save all CSVs ──────────────────────────────────────────────────────────────
save_map = {
    'seasonal_baseline_u2.csv':      seasonal,
    'event_window_stats_u2.csv':     ev_stats_all,
    'first_day_reactions_u2.csv':    first_day,
    'abnormal_returns_u2.csv':       abnormal,
    'correlation_by_window_u2.csv':  corr_win,
    'sector_heatmap_u2.csv':         heatmap_df,
    'lead_lag_ccf_u2.csv':           ccf_full,
    'rolling_betas_u2.csv':          rb if not rb.empty else pd.DataFrame(),
    'international_spillover_u2.csv': intl_s,
    'pair_trades_u2.csv':            pair_trades,
    'freight_spread_u2.csv':         freight_s,
    'regime_summary_u2.csv':         regime_s,
    'summary_key_numbers_u2.csv':    summary_df,
}

print(f'Saving CSVs → {CSV_DIR}')
for fname, df in save_map.items():
    try:
        if isinstance(df, pd.DataFrame) and not df.empty:
            df.to_csv(os.path.join(CSV_DIR, fname))
            print(f'  ✓ {fname}')
        elif isinstance(df, pd.Series) and not df.empty:
            df.to_frame().to_csv(os.path.join(CSV_DIR, fname))
            print(f'  ✓ {fname} (Series)')
        else:
            print(f'  — {fname} (empty, skipped)')
    except Exception as e:
        print(f'  ✗ {fname}: {e}')

print(f'\nDone. {len(save_map)} files → {CSV_DIR}')
```

**Output:**
```
Saving CSVs → c:\Users\User\Desktop\2026-hormuz-blockade-analysis\outputs\update_2\data
  ✓ seasonal_baseline_u2.csv
  ✓ event_window_stats_u2.csv
  ✓ first_day_reactions_u2.csv
  ✓ abnormal_returns_u2.csv
  ✓ correlation_by_window_u2.csv
  ✓ sector_heatmap_u2.csv
  ✓ lead_lag_ccf_u2.csv
  ✓ rolling_betas_u2.csv
  ✓ international_spillover_u2.csv
  ✓ pair_trades_u2.csv
  ✓ freight_spread_u2.csv
  ✓ regime_summary_u2.csv
  ✓ summary_key_numbers_u2.csv

Done. 13 files → c:\Users\User\Desktop\2026-hormuz-blockade-analysis\outputs\update_2\data
```

## Appendix A: Methodology

### A.1 Framework

Same OLS market model as Update 1, extended to 5 windows.

$$AR_{i,t} = R_{i,t} - (\hat{\alpha}_i + \hat{\beta}_i \cdot R_{m,t}) \qquad CAR_i = \sum AR_{i,t}$$

**Estimation window:** Jan 1–Feb 27, 2026 (39 trading days). Beta estimated here, applied to all four post-event windows. Caveat: escalation risk was already being priced during the estimation window — betas may understate pre-shock energy sensitivity.

**WTI/Gold regime signal:**
$$\text{Ratio}_t = \frac{P_{WTI,t}}{P_{GOLD,t}} \text{ (base 100 at Jan 1, 2026)}$$

Rising → supply shock. Falling → diplomacy/expectations.

### A.2 Window Boundaries

| Window | Dates | Driver |
|--------|-------|--------|
| Pre-event | Jan 1–Feb 27 | Escalation pricing |
| Shock | Feb 28–Apr 16 | Physical scarcity |
| False Dawn | Apr 17–Apr 30 | Peace signal |
| Paradox Correction | May 1–May 29 | Diplomacy beats supply |
| Volatile Diplomacy | Jun 1–Jun 13 | Re-escalation + talks |

### A.3 New Asset Technical Notes

| Asset | Key Caveat |
|-------|-----------|
| BWET | Launched May 2023 — 2 baseline years. AUM $24.9M. Futures (contango applies). |
| ARAMCO | Tadawul (Sun–Thu UTC+3) forward-filled to US calendar. ~18h same-day lag. |
| UNG | Nat gas futures. Roll decay in contango. Not equivalent to spot prices. |
| TLT | 20+ year duration — most sensitive to long-end yield changes. |
| ITA | Market-cap weighted. Includes both prime contractors and services firms. |
| XLE | 27 holdings including refiners, not just E&P. |

## Appendix B: Limitations That Matter

| Limitation | Impact |
|------------|--------|
| Single event study | 2026 Hormuz specifically — different topology may produce different results |
| Daily data only | Intraday lead-lag invisible · CCF lag-0 biased by WTI's 24/5 vs NYSE hours |
| 39-day beta estimation | Non-linear during extreme moves — treat magnitudes as directional bounds |
| No transaction costs | All pair trade P&Ls are pre-cost upper bounds |
| No CPI adjustment | All multi-year comparisons are nominal |
| BWET limited history | 2-year baseline vs 5 for original assets |
| ARAMCO alignment | Forward-fill introduces ~18h lag on same-day comparisons |
| Forward expectations proxy | Post-hoc binary flags — not causal identification |
| Ceasefire post-cutoff | Jun 21–25 ceasefire not included — this study ends Jun 13 |

---
*Abdallah A Khames · BODZZ · [@abdallah-bodzz](https://github.com/abdallah-bodzz) · Update 2 — Static snapshot: June 13, 2026*
